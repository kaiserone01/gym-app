# Handoff — gym-app

## 1. Objetivo
Panel administrativo (`apps/web-admin`) de un sistema de gestión de gimnasio: miembros, pagos, y un módulo de Caja (apertura/cierre de turno con arqueo, en USD y bolívares).

## 2. Estado actual
- **Daemon de la tasa BCV ELIMINADO del repo (a pedido del usuario, no solo pausado):** `apps/worker/src/daemon.ts`, el script `daemon`/dependencia `node-cron` en `apps/worker/package.json`, y el script `worker:daemon` en el `package.json` raíz ya no existen. Sigue existiendo `apps/worker/src/actualizar-tasa.ts` (script manual, `npm run actualizar-tasa`), que es el esquema previo y el único mecanismo soportado para actualizar la tasa. No hay ninguna automatización de tasa BCV corriendo ni planeada — si se pide de nuevo, hay que recrear el daemon desde cero (ver commit revertido `eff0bfc` en el historial de git para referencia).
**Funciona (verificado con build + typecheck; parte de esto además confirmado en vivo por el usuario):**
- Apertura y cierre de turno de caja, con registro de pagos y egresos durante el turno vía modales (`ModalRegistrarPagoCaja.tsx` wizard de 4 pasos, `ModalRegistrarEgreso.tsx`).
- Cuadre de caja por método de pago (efectivo USD/Bs y métodos bancarios), mostrando el monto esperado y su referencia en USD cuando el monto es en bolívares (`Bs. X.XXX,XX (REF $Y.YY)`), detectando la moneda desde la transacción real (`Pago.montoBs`/`Egreso.moneda`), no desde el nombre del método.
- **Fix crítico confirmado por el usuario en producción:** `cerrarTurnoAction` guardaba el arqueo de cierre VACÍO (leía un catálogo de métodos viejo/desactualizado en vez de los métodos reales del turno) — ya corregido, el arqueo ahora se guarda bien.
- No se puede inscribir un miembro nuevo ni registrar un pago de mensualidad si no hay un turno de caja abierto (bloqueo con aviso y botón directo a "Abrir turno").
- Al abrir un nuevo turno, se muestra una tarjeta "Último cierre" con el efectivo contado (USD/Bs) del cierre anterior de esa sucursal, y los campos de fondo inicial se precargan con esos valores (editables).
- Layout de `/caja` reorganizado: barra compacta (estado + botones "Registrar pago"/"Registrar egreso", ambos abren modal), resumen por método en grilla de 3 columnas, "Pagos del turno"/"Egresos del turno" lado a lado con scroll interno propio (se activa ~7-8 filas, `max-h-80` = 320px), arqueo de cierre al final. El botón "Usar montos esperados" (agregado externamente, fuera de esta sesión) es verde/acento igual que "Registrar pago".
- Modal de registrar pago en Caja: título centrado, letra más grande (prop `grande` en `BuscadorMiembro` y `SelectorMetodoPago` para no afectar sus otros usos en `/miembros` y `/pagos/nuevo`), modal más ancho (`max-w-2xl`).
- `CurrencyInput` (componente de moneda con formato `1.234,56` y máscara USD/Bs) corregido — ya no revierte lo que el usuario escribe en modo controlado (ver Intentos fallidos, bug de tooling que causó confusión al diagnosticarlo).
- El badge de reloj + tasa BCV (esquina superior derecha, `fixed`) ya no se monta sobre los controles de cada página (`pt-16` en el `<main>` del layout).
- **Fix de sesión rota:** `Sesion.sucursalActivaId` es opcional en la base (columna `String?`) pero el código lo trataba como no-nulo con un `!` — una sesión vieja (creada antes de que el login exigiera elegir sucursal) causaba un 500 en cualquier página del panel. `validarSesion` ahora trata esa sesión como inválida y fuerza un nuevo login.
- `CLAUDE.md` con las reglas de trabajo (reuso de código, no usar Chrome DevTools MCP salvo pedido explícito, commits sin firma/en español/solo resumen de una línea, protocolo de handoff).
- **Botón "Imprimir" en `/miembros`** (verificado en vivo por el usuario, ya en `main`): imprime solo la lista de miembros (tabla simple en blanco y negro, sin fotos ni acciones) — sidebar, filtros y navegación quedan ocultos con `print:hidden`.
- **Botón "Imprimir" en `/pagos` (Histórico de Pagos), mismo patrón, reusando `BotonImprimir`:** se agregó una lista plana de pagos del período (Fecha, Miembro, Método, USD, Tasa, Bs), visible siempre en pantalla arriba del detalle por turno; al imprimir, solo esa lista queda visible (el detalle por turno con arqueo/egresos, los filtros y el resumen de total quedan `print:hidden`). **Sin verificar todavía en vivo por el usuario.**
- **`/usuarios` en tarjetas, separado en dos columnas** (`lg:grid-cols-2`): "Socios, gerentes y recepción" a la izquierda, "Entrenadores" a la derecha — reemplaza la tabla de escritorio + lista mobile duplicadas que había antes.
- **Entrenadores ya no reciben permisos del panel:** `PERMISOS_POR_ROL.ENTRENADOR` vacío en `CrearUsuarioAdmin.ts` (y su duplicado en `packages/db/backfillPermisosYSucursales.ts`), la sección "Permisos" no se renderiza en `/usuarios/[id]` para rol ENTRENADOR, y `actualizarPermisosUsuarioAction` la rechaza también a nivel de dominio (`RolSinPermisosError`) por si se intenta igual. **Entrenadores ya existentes con permisos viejos (VER en Miembros/Pagos/Planes) NO se tocaron automáticamente** — hay un script nuevo para limpiarlos, ver Próximos pasos.
- **"Dar de baja" reemplazado por "Eliminar" (papelera) en `/usuarios`:** el botón de baja/reactivar se sacó de `/usuarios/[id]`; ahora cada tarjeta de usuario tiene un ícono de papelera (`TarjetaUsuario.tsx`) que llama a `eliminarUsuarioAction` (borrado físico real, con confirmación). Si el usuario tiene turnos/pagos/miembros asociados, la base rechaza el borrado por integridad referencial y se muestra un error legible en vez de romper (`EliminarUsuarioAdmin.ts` → `UsuarioConHistorialError`) — en ese caso el usuario queda igual que antes, no hay forma de "ocultarlo" ya que se quitó dar de baja. **Sin verificar todavía en vivo por el usuario** (ni el flujo de eliminar un usuario sin historial, ni el mensaje de error cuando sí tiene historial).
- **"Nuevo usuario" subido a la fila de tabs en `/usuarios`** (ya no repite el título "Usuarios" dos veces) — `TabsConfiguraciones` ahora acepta una prop opcional `accion` para esto.
- **Cambio de plan a mitad de ciclo cobrando solo la diferencia (caso real: Juanito, cédula 585956, cambió de "Sin entrenador" $25 a "Con entrenador" $30 sin que venciera).** Nuevo caso de uso `CambiarPlanConPago.ts` + Server Action `cambiarPlanAction` (`pagos/actions.ts`) + UI `FormularioCambiarPlan.tsx`, con una card nueva "Cambiar de plan" en `/miembros/[id]` (solo visible si el ciclo actual no venció y hay turno abierto). Cobra `max(0, precioNuevo - precioViejo)` — el precio "viejo" sale del plan real de la Suscripción activa, no de `Miembro.planId` (que puede desincronizarse). **El vencimiento NO se mueve** — el miembro ya pagó esos días, el cambio de plan no se los renueva (decisión del usuario). Si no hay ciclo vigente (nunca pagó o ya venció), no se ofrece esta opción — ahí corresponde un pago normal por el precio completo. **Sin verificar todavía en vivo.**
- **Fix del bug reportado ("volvió a cobrar $25 después de cambiar a $30"):** causa raíz identificada — `actualizarMiembroAction` (editar miembro) solo hacía `revalidatePath("/miembros")`, no de la página específica `/miembros/{id}`, así que el formulario de "Registrar pago" de esa misma pantalla podía seguir mostrando el plan/monto viejos (Router Cache de Next.js) aunque la base ya tuviera el plan nuevo. Se agregó `revalidatePath(\`/miembros/${id}\`)`. Además, `ActualizarMiembro.ts` (el flujo gratuito de "editar miembro" → cambiar plan) ajustaba la fecha de vencimiento pero nunca actualizaba el `planId` de la Suscripción activa — quedaba desincronizada del plan real del miembro. Ahora `ActualizarMiembro.ts` también llama a `suscripciones.cambiarPlan(...)` para mantenerlos en sync (nuevo método en `ISuscripcionRepository`/`PrismaSuscripcionRepository`). **Sin verificar todavía en vivo que el fix resuelve el caso real de Juanito.**
- **"Editar" agregado a las cards de `/miembros`** (antes solo estaba en la vista de tabla/lista) y **cursor de manito (`cursor-pointer`) en el toggle Activo/Inactivo** (`EstadoToggle.tsx`) — antes mostraba el cursor default del navegador.
- **Resuelto: había dos botones "Cambiar plan" en `/miembros/{id}` al mismo tiempo** (el gratis del formulario principal — solo mueve fecha, no cobra — y el nuevo de "Cambiar de plan" que cobra diferencia), causando confusión sobre cuál usar y si hacía falta "Guardar" antes de registrar el pago. Se resolvió ocultando el botón/editor de plan gratis en `FormularioMiembro.tsx` mientras el ciclo esté vigente (`tieneCicloVigente`) — en ese caso solo existe la vía paga del panel derecho; el editor gratis sigue disponible únicamente cuando NO hay ciclo vigente (nunca pagó o ya venció), que es su único uso real ahora. Se agregó una nota aclaratoria arriba del panel derecho: "Registrar pago y Cambiar de plan se aplican al instante, no hace falta tocar Guardar primero". **Sin verificar todavía en vivo.**
- **Se quitó la opción "Personalizado" del selector de plan** (en alta y edición de miembro) — a pedido del usuario. Antes creaba/reusaba un Plan ad-hoc llamado "Personalizado" por organización+frecuencia+entrenador (`obtenerOCrearPlanPersonalizado` en `miembros/actions.ts`, ya eliminado). Miembros que YA tenían un plan "Personalizado" asignado no se tocaron — ese Plan sigue existiendo en `/planes` como cualquier otro y se puede dar de baja desde ahí si ya no se usa.
- **Cambio de plan a mitad de ciclo: bloqueado entre frecuencias distintas.** El usuario probó pasar de un plan Semanal a uno Mensual cobrando la diferencia — con el diseño original eso hubiera dejado el vencimiento sin mover (fecha de semanal) pese a cobrar precio de mensual. Se agregó `FrecuenciaDistintaError` en `CambiarPlanConPago.ts`: el selector de "Cambiar de plan" (`FormularioCambiarPlan.tsx`) ahora solo lista planes de la MISMA frecuencia que el actual, con una nota explicando que para cambiar de frecuencia corresponde un pago normal (`Registrar pago`) por el precio completo. **Sin verificar todavía en vivo.**
- **"Dar de baja"/"Reactivar" reemplazado por "Eliminar" (papelera) también en `/planes`** — mismo patrón que ya se hizo en `/usuarios`: cada fila/card de la lista de planes (`/planes`) tiene ahora un ícono de papelera (`BotonEliminarPlan.tsx`) que llama a `eliminarPlanAction` (borrado físico real, con confirmación, requiere permiso `PLANES`/`ELIMINAR`). Se quitó el botón "Dar de baja"/"Reactivar" de `/planes/[id]`. Si el plan tiene miembros o suscripciones usándolo, la base rechaza el borrado por integridad referencial y se muestra un error legible (`EliminarPlan.ts` → `PlanConHistorialError`) — en ese caso el plan queda igual que antes; ya no hay forma de "ocultarlo" desactivándolo (mismo trade-off que se aceptó para usuarios). **Sin verificar todavía en vivo** — el usuario pidió esto específicamente para poder borrar los planes "$25 Sede Única" y "$35 VIP Multi-sede" directo desde la lista, sin usar más "Dar de baja".
- **"Registrar pago" y "Cambiar de plan" unificados en un panel con pestañas** en `/miembros/{id}` — el usuario reportó que al elegir un plan con entrenador en "Cambiar de plan" aparecía una SEGUNDA grilla de métodos de pago debajo de la de "Registrar pago", dando la sensación de que había que pagar dos veces. Se armaron dos mockups (Opción A: pestañas, Opción B: colapsable) y el usuario eligió la A. Nuevo componente `PanelPagoYCambioPlan.tsx`: una sola `Card` con pestañas "Registrar pago" / "Cambiar de plan" (solo si `tieneCicloVigente`; si no hay ciclo vigente no hay pestañas, se muestra directo "Registrar pago" como antes) — nunca se ven las dos grillas de método de pago a la vez. Reemplaza las dos `<Card>` que estaban antes en `miembros/[id]/page.tsx`. **Sin verificar todavía en vivo.**

**Pendiente / decisiones tomadas por el usuario:**
- **Constancia/resumen al cerrar turno:** el usuario preguntó si convenía agregar una pantalla de "resumen del turno anterior" al cerrar. Recomendación dada: NO agregarla — sería redundante con la tarjeta "Último cierre" que ya se muestra al ABRIR el siguiente turno (que es el momento/persona correcta para necesitar ese dato). El usuario no pidió implementarla — **queda sin hacer, a propósito**.
- **Actualización automática de la tasa BCV — el daemon 24/7 en Easypanel sigue DESCARTADO, pero el 2026-09-26 el usuario pidió explícitamente un enfoque nuevo y distinto: refresco perezoso durante el uso del panel, sin daemon, sin servicio nuevo, sin CI.** Ver sección siguiente para el detalle del daemon descartado, y la sección 3 ("Etapa 1 — tasa BCV automática, 2026-09-26") para el enfoque nuevo, en progreso. Plan completo en `docs/superpowers/plans/2026-09-26-tasa-bcv-automatica.md`.

## 2.1. Intento de despliegue del daemon de tasa BCV (pausado, sin efecto)
Contexto: la tasa BCV mostrada en el panel estaba desactualizada 11 días (832.49 en vez de 852.41 real) porque nadie corría `npm run actualizar-tasa` manualmente. Se corrigió el valor a mano una vez (ya está en la base, actualizado). Para que no vuelva a pasar, se creó `apps/worker/src/daemon.ts` (long-running, corre la actualización al arrancar y luego todos los días a las 12:00 UTC vía `node-cron`) y se agregó el script `worker:daemon` en `package.json`/`apps/worker/package.json`.

**Se intentó desplegar este daemon como servicio nuevo en Easypanel ("tasa-bcv") y se pausó a mitad de camino, a pedido del usuario — no está desplegado, no está corriendo.** Lo que se descubrió durante el intento, para la próxima vez que se retome:
- El proyecto de Easypanel se llama "platforms"; los servicios existentes son `bd_gym_pg`, `contigo-pgdb`, `contigo-platform`, `disico-dashboard`, `encuentrove`, `gym-app` (el panel admin real — "Caja" es solo el nombre de una PÁGINA dentro de esa app, no el nombre del servicio de Easypanel), `gym-kiosk`, `newferapp`, `smart-form-iul`, `smart-form-iul-pg`, `sweetsin-postgres-api`, `sweetsin-web`, `tasa-bcv` (el que se estaba creando), `whatsapp-worker`.
- El servicio `gym-app` en Easypanel NO usa variables de entorno sueltas cargadas en la UI — usa un archivo `.env` cifrado con **dotenvx** (ver el log: `injected env (0) from ../../.env // tip: encrypted .env [www.dotenvx.com]`). Ese `.env` NO está en el repo de GitHub (`.gitignore` tiene `.env*`) — se subió manualmente a Easypanel en algún momento anterior, fuera de esta sesión.
- El servicio `gym-app` en Easypanel, en su pantalla de acciones en vivo, solo tiene: Detener, Reiniciar, Registros, Consola, Abrir, Notas, Forzar reconstrucción, Habilitar despliegue automático, Destruir — NO tiene un botón visible de "Variables de entorno" ahí. La pantalla de "Variables de entorno" a nivel de PROYECTO ("platforms") está vacía y además tiene un aviso de que hace falta una licencia con "Access Control" para gestionar miembros del proyecto — no es el lugar correcto para esto.
- No se llegó a encontrar dónde exactamente se carga/sube ese `.env` cifrado a `gym-app` en la UI de Easypanel — quedó sin resolver.
- El usuario compartió en el chat (texto plano) la `DATABASE_URL` real de producción y las claves de R2 de su `.env` local, para poder usarlas en el daemon. **Son credenciales reales, ya expuestas en el historial de este chat** — si se retoma este tema, no volver a pedírselas por chat; usar lo que ya se tiene o pedirle que las cargue directo en el campo correspondiente de Easypanel cuando se encuentre.
- El servicio "tasa-bcv" en Easypanel quedó CREADO (tipo Aplicación, Nixpacks, comando de instalación `npm ci`, comando de inicio `npm run worker:daemon`, repo `kaiserone01/gym-app` rama `main`, ruta de compilación `/`) pero **sin variables de entorno configuradas y sin desplegar** — no se le dio "Implementar" nunca. Se puede seguir configurando ese mismo servicio cuando se retome, no hace falta crear uno nuevo.

## 3. Archivos y cambios (todas las sesiones recientes acumuladas)
- `packages/ui/components/CurrencyInput.tsx` — fix de condición de carrera en modo controlado (`useRef` para distinguir "el padre me devolvió lo mismo que emití" de "el padre cambió el valor por su cuenta").
- `apps/web-admin/app/(panel)/caja/page.tsx` — tarjeta "Último cierre" al abrir turno; layout reorganizado (barra compacta, resumen en grilla, pagos/egresos en 2 columnas con scroll interno).
- `apps/web-admin/app/(panel)/caja/FormularioAbrirTurno.tsx` — muestra "Último cierre" y precarga fondo inicial; fetch a `/api/caja/ultimo-cierre` si hay que elegir sucursal.
- `apps/web-admin/app/api/caja/ultimo-cierre/route.ts` — endpoint GET, valida sucursal visible para el usuario.
- `packages/domain/use-cases/ObtenerUltimoCierrePorSucursal.ts` — trae el último `Turno` cerrado de una sucursal y su efectivo contado.
- `packages/domain/ports/ITurnoRepository.ts` / `packages/infrastructure/persistence/prisma/PrismaTurnoRepository.ts` — `buscarUltimoCerradoPorSucursal`.
- `apps/web-admin/app/(panel)/caja/actions.ts` — **fix crítico**: `cerrarTurnoAction` ahora lee los métodos reales desde `formData.getAll("metodos")` en vez de un catálogo estático desactualizado.
- `apps/web-admin/app/(panel)/caja/FormularioArqueo.tsx` — manda los métodos reales como campos ocultos; botón "Usar montos esperados" ahora usa el componente `Button` compartido (verde/acento).
- `apps/web-admin/app/(panel)/layout.tsx` — `pt-16` para el badge `RelojYTasa`.
- `apps/web-admin/app/(panel)/miembros/page.tsx`, `.../nuevo/page.tsx`, `.../[id]/page.tsx` — bloquean inscripción/pago sin turno abierto (`AvisoCajaCerrada.tsx`, `obtenerTurnoAbiertoParaUsuario.ts`).
- `apps/web-admin/app/(panel)/caja/BotonRegistrarEgreso.tsx` / `ModalRegistrarEgreso.tsx` — nuevo modal de egreso (reemplazó un formulario siempre visible).
- `apps/web-admin/app/(panel)/caja/ModalRegistrarPagoCaja.tsx` — título centrado, letra más grande, modal más ancho.
- `apps/web-admin/app/(panel)/caja/SelectorMiembroModal.tsx` — `BuscadorMiembro` recibe prop opcional `grande` (solo para el modal de Caja, no afecta `/miembros` ni `/pagos/nuevo`).
- `apps/web-admin/app/(panel)/pagos/SelectorMetodoPago.tsx` — mismo patrón, prop opcional `grande`.
- `packages/domain/entities/Sesion.ts` / `packages/domain/use-cases/ValidarSesion.ts` / `packages/infrastructure/persistence/prisma/PrismaSesionRepository.ts` — **fix crítico**: `sucursalActivaId` puede ser `null` (sesión vieja); `validarSesion` la invalida y fuerza login en vez de tronar con 500.
- `apps/worker/src/daemon.ts` (nuevo, **sin desplegar, ver sección 2.1**) + `apps/worker/package.json` / `package.json` raíz (script `worker:daemon`) — daemon con cron diario para la tasa BCV.
- Se eliminaron de la base de datos real los usuarios de prueba `nuevo@gymdemo.com`, `recepcion-1789272232632@gymdemo.com`, `gerente@gymdemo.com` y todo lo asociado (turnos, pagos, egresos, arqueo, permisos).
- Se corrigió manualmente la `TasaCambio` desactualizada (852.4168, corrida una vez con `npm run actualizar-tasa`) — esto SÍ es efectivo y ya está en la base real, independiente de que el daemon automático no se haya desplegado.
- `CLAUDE.md` — reglas de trabajo de la sesión.
- `apps/web-admin/app/(panel)/layout.tsx` — sidebar y `NavegacionMobile` con `print:hidden`.
- `apps/web-admin/app/(panel)/miembros/ListaMiembros.tsx` — nuevo componente `VistaImpresion` (tabla blanco y negro, sin fotos/acciones), visible solo con `print:block`; las vistas Cards/Lista de pantalla ahora son `print:hidden`.
- `apps/web-admin/app/globals.css` — `@media print { body { background: #ffffff } }` (el tema oscuro del panel no se imprime).
- `apps/web-admin/app/(panel)/pagos/page.tsx` — botón `BotonImprimir` junto al título (reusado tal cual, sin duplicar); nueva lista plana de pagos del período (`pagosOrdenados`, combina `reporte.turnos` + `reporte.ajustesFueraDeTurno`, ordenada por `fechaPago` desc); filtros, resumen de total y detalle por turno marcados `print:hidden`.
- `apps/web-admin/app/(panel)/usuarios/page.tsx` — tabla de escritorio + lista mobile reemplazadas por un grid de 2 columnas (`personal` vs `entrenadores`, filtrados por `rol`), usando el nuevo `TarjetaUsuario`.
- `apps/web-admin/app/(panel)/usuarios/TarjetaUsuario.tsx` (nuevo) — card de usuario con ícono de papelera (`eliminarUsuarioAction`, con `window.confirm` y `useFeedback` para el error, mismo patrón que `EstadoToggle` en Miembros).
- `apps/web-admin/app/(panel)/usuarios/[id]/page.tsx` — se quitó el form de Dar de baja/Reactivar; la sección "Permisos" ahora es condicional (`rol !== "ENTRENADOR"`), con un texto explicativo en su lugar para entrenadores.
- `apps/web-admin/app/(panel)/usuarios/actions.ts` — nueva `eliminarUsuarioAction` (sin redirect, pensada para llamarse desde el cliente y no desde un `<form>`); se borraron `darDeBajaUsuarioAction`/`reactivarUsuarioAction`/`cambiarEstadoUsuario` (ya sin uso).
- `packages/domain/use-cases/EliminarUsuarioAdmin.ts` (nuevo) — valida rol SOCIO, no auto-eliminación, y traduce cualquier fallo de integridad referencial (turnos/pagos/miembros asociados) a `UsuarioConHistorialError` con mensaje legible.
- `packages/domain/use-cases/ActualizarPermisosUsuario.ts` — nuevo `RolSinPermisosError`: rechaza asignar permisos a un usuario con rol ENTRENADOR.
- `packages/domain/use-cases/CrearUsuarioAdmin.ts` / `packages/db/backfillPermisosYSucursales.ts` — `PERMISOS_POR_ROL.ENTRENADOR` ahora es `[]` en los dos lugares (matriz duplicada a propósito, ver comentario en el archivo).
- `packages/domain/ports/IUsuarioAdminRepository.ts` — comentario de `eliminar()` actualizado (ya no es solo para compensar creación fallida).
- `packages/db/quitarPermisosEntrenadores.ts` (nuevo, script manual, `npm run db:quitar-permisos-entrenadores --workspace packages/db`) — limpia los permisos que hayan quedado en entrenadores creados ANTES de este cambio. No se corrió todavía contra la base real (esta sesión no tiene acceso al `.env` de producción).
- `apps/web-admin/app/(panel)/configuraciones/TabsConfiguraciones.tsx` — nueva prop opcional `accion` (botón a la derecha de la fila de tabs).
- `apps/web-admin/app/(panel)/usuarios/layout.tsx` — calcula `puedeCrear` y pasa el botón "Nuevo usuario" como `accion` de `TabsConfiguraciones`.
- `apps/web-admin/app/(panel)/usuarios/page.tsx` — se quitó el título "Usuarios" y el botón (ahora vive en el layout).
- `packages/domain/use-cases/CambiarPlanConPago.ts` (nuevo) — cambia de plan a mitad de ciclo cobrando `max(0, precioNuevo - precioViejo)` sin extender el vencimiento; usa el plan real de la Suscripción activa (no `Miembro.planId`) como precio "viejo"; si no hay ciclo vigente, tira `SinCicloVigenteError`.
- `packages/domain/ports/ISuscripcionRepository.ts` / `packages/infrastructure/persistence/prisma/PrismaSuscripcionRepository.ts` — nuevo método `cambiarPlan(id, planId)` (cambia el plan de una Suscripcion sin tocar `inicio`/`fin`).
- `packages/domain/use-cases/ActualizarMiembro.ts` — al cambiar de plan desde "editar miembro" (el flujo gratuito, sin pago), ahora también llama a `suscripciones.cambiarPlan(...)` para que la Suscripción activa quede sincronizada con el plan nuevo (antes solo le ajustaba la fecha, no el `planId` — esa desincronización fue la causa raíz del bug reportado).
- `apps/web-admin/app/(panel)/pagos/actions.ts` — nueva `cambiarPlanAction` (mismo patrón que `registrarPagoAction`).
- `apps/web-admin/app/(panel)/miembros/FormularioCambiarPlan.tsx` (nuevo) — formulario de la card "Cambiar de plan": elige plan nuevo, muestra la diferencia calculada en el cliente, pide método de pago solo si hay diferencia > 0.
- `apps/web-admin/app/(panel)/miembros/[id]/page.tsx` — agrega la card "Cambiar de plan" (solo si `tieneCicloVigente` y hay turno abierto).
- `apps/web-admin/app/(panel)/miembros/actions.ts` — `actualizarMiembroAction` ahora también hace `revalidatePath(\`/miembros/${id}\`)` (antes solo `/miembros`), fix del bug de la página quedando con datos viejos tras editar el plan.
- `apps/web-admin/app/(panel)/miembros/ListaMiembros.tsx` — las cards de `/miembros` (`VistaCards`) ahora tienen un link "Editar" visible (antes solo estaba en la vista de lista/tabla).
- `apps/web-admin/app/(panel)/miembros/EstadoToggle.tsx` — `cursor-pointer` (y `cursor-not-allowed` mientras está pendiente) en el switch Activo/Inactivo.
- `apps/web-admin/app/(panel)/miembros/FormularioMiembro.tsx` — nueva prop `tieneCicloVigente`; el botón/editor gratis de "Cambiar plan" ahora solo se muestra cuando NO hay ciclo vigente (o en alta); se eliminó por completo la opción "Personalizado" (estado, botón en la grilla, fieldset de frecuencia/entrenador).
- `apps/web-admin/app/(panel)/miembros/actions.ts` — se borró `obtenerOCrearPlanPersonalizado` (ya sin uso) y se simplificó `resolverPlanId` a solo leer `formData.get("planId")`.
- `packages/domain/use-cases/CambiarPlanConPago.ts` — nuevo `FrecuenciaDistintaError`: rechaza cobrar diferencia entre planes de distinta frecuencia.
- `apps/web-admin/app/(panel)/miembros/FormularioCambiarPlan.tsx` — nueva prop `frecuenciaActual`; el selector de "Plan nuevo" filtra a `planesMismaFrecuencia`, con nota explicativa si hay planes ocultos por frecuencia distinta.
- `apps/web-admin/app/(panel)/miembros/[id]/page.tsx` — calcula `frecuenciaActual` (del plan real del miembro) y se la pasa a `FormularioCambiarPlan`.
- `packages/domain/use-cases/EliminarPlan.ts` (nuevo) — valida permiso `PLANES`/`ELIMINAR`, existencia del plan, y traduce cualquier fallo de integridad referencial (miembros/suscripciones usándolo) a `PlanConHistorialError`.
- `packages/domain/ports/IPlanRepository.ts` / `packages/infrastructure/persistence/prisma/PrismaPlanRepository.ts` — nuevo método `eliminar(id)` (borrado físico).
- `apps/web-admin/app/(panel)/planes/actions.ts` — nueva `eliminarPlanAction` (sin redirect, pensada para el cliente); se borraron `darDeBajaPlanAction`/`reactivarPlanAction` (ya sin uso).
- `apps/web-admin/app/(panel)/planes/BotonEliminarPlan.tsx` (nuevo) — papelera con `window.confirm`, mismo patrón que `TarjetaUsuario`/`EstadoToggle`.
- `apps/web-admin/app/(panel)/planes/page.tsx` — agrega `BotonEliminarPlan` a cada fila (tabla de escritorio) y card (mobile).
- `apps/web-admin/app/(panel)/planes/[id]/page.tsx` — se quitó el botón "Dar de baja"/"Reactivar".
- `apps/web-admin/app/(panel)/miembros/PanelPagoYCambioPlan.tsx` (nuevo) — panel con pestañas que envuelve `FormularioPago` y `FormularioCambiarPlan` dentro de una sola `Card`, mostrando solo una grilla de método de pago a la vez.
- `apps/web-admin/app/(panel)/miembros/[id]/page.tsx` — usa `PanelPagoYCambioPlan` en vez de las dos `<Card>` separadas de "Registrar pago" y "Cambiar de plan".
- **Segundo intento de reorganizar "Editar miembro" — esta vez en 3 columnas, con mockup aprobado por el usuario ANTES de tocar código** (`https://claude.ai/artifact/KoF1pdzeu3spnr96cKjRXa`): Datos personales | Plan de membresía | Registrar pago/Cambiar de plan, y Ciclos como fila completa abajo (restyleado como lista con divisores, no tarjetas individuales). A diferencia del primer intento (revertido), esta vez "Plan de membresía" NUNCA sale del árbol DOM del `<form id="formulario-miembro">` — se usa `style={{display: "contents"}}` en el propio `<form>` para que sus dos cards (Datos personales, Plan de membresía) se vuelvan celdas independientes de la grilla CSS de 3 columnas del contenedor padre, sin dejar de ser hijos reales del formulario. Solo "Registrar pago/Cambiar de plan" (`panelLateral`, con sus propios `<form>` separados) y "Ciclos" (sin campos) quedan fuera del `<form>`, como siempre. `apps/web-admin/app/(panel)/miembros/FormularioMiembro.tsx` — grilla reordenada, botón "Cambiar entrenador" renombrado a **"Asignar entrenador"** (a pedido del usuario), nota de "Guardar" actualizada para aclarar qué guarda y qué no.
- **Nuevo `apps/web-admin/app/(panel)/miembros/ContextoCambiosSinGuardar.tsx`** (Context de React) — expone si hay cambios sin guardar en "Datos personales" (comparando el estado local contra `valoresIniciales`: nombre, celular, fecha, sede, entrenador, foto, o un cambio de plan gratis a medio confirmar) a cualquier componente hijo, sin acoplar `FormularioMiembro` con `FormularioCambiarPlan` (viven en árboles separados de React, pero comparten pantalla).
- **`cambiarPlanAction` (el botón "Cobrar diferencia y cambiar plan") ahora redirige a `/miembros` con mensaje de éxito** (`conMensajeOk`, mismo patrón que `actualizarMiembroAction`/`registrarPagoAction`) en vez de quedarse en la misma pantalla — a pedido explícito del usuario, para no tener que tocar "Guardar" después de cambiar el plan. `apps/web-admin/app/(panel)/pagos/actions.ts` — se quitó el campo `ok` de `EstadoCambioPlan` (ya no se usa, la acción siempre redirige en éxito). `apps/web-admin/app/(panel)/miembros/FormularioCambiarPlan.tsx` — usa `useHayCambiosSinGuardar()` y, si hay cambios pendientes en Datos personales, pide confirmación (`window.confirm`) antes de enviar el formulario (que redirige y los perdería); se quitó el `useEffect` muerto que mostraba `estado.ok` (ya no llega, ahora la acción redirige). **Sin verificar todavía en vivo — recién implementado.**
- **Fix de ancho: las 3 columnas se veían apretadas en vivo** (texto cortado, botones partidos en dos líneas) — el contenedor de `/miembros/{id}` y `/miembros/nuevo` seguía en `max-w-4xl` (896px, heredado de cuando era 1 columna + aside angosto). Se subió a `max-w-7xl` (1280px) en ambas páginas.
- **Ajustes tras ver la pantalla ancha en vivo (4 pedidos del usuario en un solo mensaje):**
  1. **"Sube las 3 cards más arriba, que no tenga que hacer scroll down"** — se redujo el espacio arriba del formulario en `[id]/page.tsx` y `nuevo/page.tsx`: `p-6 lg:p-8` → `p-6 lg:px-8 lg:py-6`, y el margen bajo el título `mb-6` → `mb-4`.
  2. **"El botón Guardar se quedó pequeño"** — al mover "Guardar" adentro de la card "Datos personales" (que ya no es un contenedor flex de ancho completo como el `<form>` viejo), el botón dejó de estirarse solo. Se le agregó `w-full` explícito en `FormularioMiembro.tsx`.
  3. **Bug real: al subir de plan a uno CON entrenador desde "Cambiar de plan" (cobrar diferencia), nunca había forma de asignar el entrenador** — `cambiarPlanConPago` (el caso de uso del cobro de diferencia) nunca tocaba `entrenadorId`, a diferencia del flujo gratuito de "editar miembro". Como además `cambiarPlanAction` ahora redirige a `/miembros` al instante, el usuario terminaba sin ningún paso para elegir entrenador. Se agregó: `entrenadorId` a `DatosCambiarPlanConPago` (nuevo error `EntrenadorRequeridoError` si el plan nuevo incluye entrenador y no se mandó uno), y `deps.miembros.actualizar(...)` ahora también actualiza `entrenadorId` cuando `planNuevo.incluyeEntrenador` es true (se ignora si no aplica, para no pisar el entrenador de un plan que no lo requiere). En la UI, `FormularioCambiarPlan.tsx` ahora muestra un `<select>` de "Entrenador" cuando el plan elegido lo requiere, y bloquea el envío hasta elegir uno (`puedeEnviar`). Se agregó `incluyeEntrenador` a `PlanParaCambio`/`PlanParaSelector` y se enhebraron `entrenadores`/`entrenadorActualId` por toda la cadena: `[id]/page.tsx` (calcula `entrenadoresDelMiembro` con el mismo criterio de sede que `FormularioMiembro`) → `PanelPagoYCambioPlan.tsx` → `FormularioCambiarPlan.tsx`.
  4. **"En Datos personales, deja nomás 'Seleccionar archivo' — el texto de 'es la foto que aparece...' se sale del recuadro"** — se quitó ese `<span>` de ayuda (se salía de la card en columnas angostas); queda solo la etiqueta "Foto de perfil" + el input nativo (con `w-full`/`min-w-0` para que no desborde).
  **Ninguno de estos 4 ajustes está verificado en vivo todavía** — recién implementados.
- **Badge rojo "VENCIDO" en `/miembros`** — el usuario preguntó si convenía pintar toda la ficha de rojo o poner un indicador cuando la mensualidad venció; se recomendó un badge chico (mismo componente `Badge` que ya se usa para "VIGENTE"/"VENCIDO" en Ciclos) en vez de pintar toda la card, para no competir visualmente con el rojo que ya se usa para "Inactivo" — el usuario lo aprobó. `apps/web-admin/app/(panel)/miembros/ListaMiembros.tsx`: nuevo helper `estaVencido(fechaVencimiento)` (reusa `diasHastaVencimiento` de `vencimiento.tsx`, ya existente); en `VistaCards` el badge aparece debajo de `EstadoToggle` (esquina superior derecha de la card); en `VistaLista` aparece al lado de `EstadoToggle` en la columna Estado. No se tocó `VistaImpresion` (blanco y negro, sin colores). **Sin verificar todavía en vivo.**
- **Roadmap del cliente, punto (a): Plan "Diario" en frecuencia de pago.** `FrecuenciaPago` (`packages/domain/entities/Plan.ts`) pasó de `"SEMANAL" | "QUINCENAL" | "MENSUAL"` a incluir `"DIARIO"` (duración 1 día en `DURACION_DIAS_POR_FRECUENCIA`) — como `prorratearVencimiento`, `RegistrarPago.ts` y `calcularProyeccionRenovacion` ya eran genéricos (usan el mapa, sin `switch`/`case` hardcodeado por frecuencia), no hizo falta tocar esa lógica de negocio, solo el tipo y sus 4 tablas de etiquetas en la UI (`ETIQUETA_FRECUENCIA` en `FormularioMiembro.tsx`, `ModalRegistrarPagoCaja.tsx`, `planes/page.tsx`, `planes/FormularioPlan.tsx`) y los dos `<select>` de frecuencia en `FormularioPlan.tsx` (crear plan nuevo, y "Nueva frecuencia" del modal de cambio masivo). **`packages/db/sembrarMiembrosPrueba.ts` (script de datos de prueba) se dejó intacto a propósito** — tiene su propio union type local `"SEMANAL" | "QUINCENAL" | "MENSUAL"` sin importar `FrecuenciaPago` del dominio, así que sigue compilando igual y no forma parte del alcance pedido. **Importante — requiere migración real de base de datos:** como `FrecuenciaPago` es un enum de Postgres (no un string libre), se agregó `packages/db/prisma/migrations/20260924000000_frecuencia_diario/migration.sql` (`ALTER TYPE "FrecuenciaPago" ADD VALUE 'DIARIO';`) escrita a mano (esta sesión no tiene `DATABASE_URL` real para correr `prisma migrate dev`). **Esta migración NO se corrió contra producción** — el usuario tiene que correr `npx prisma migrate deploy` (desde `packages/db`, con el `DATABASE_URL` real disponible) ANTES o durante el próximo deploy, si no el enum de la base real no va a tener `'DIARIO'` y crear un plan con esa frecuencia va a fallar aunque el código ya esté desplegado. El cliente Prisma generado (`generated/prisma`, gitignored) se regenera solo en cada `npm install` gracias al postinstall del paquete `prisma` — no hace falta correr `prisma generate` a mano. **✅ VERIFICADO EN VIVO por el usuario** — creó un plan "Diario" a $3 y un miembro con ese plan, todo funcionó sin errores. Punto (a) del roadmap del cliente CERRADO.

- **Bug real en producción al crear el plan "Diario": pantalla de error genérica de Next.js ("This page couldn't load").** Reproducido y confirmado por completo: `crearPlanAction` (`planes/actions.ts`) no tiene try/catch, así que el error crudo de Postgres (`invalid input value for enum "FrecuenciaPago": "DIARIO"`) tira sin capturar y Next.js muestra su pantalla de error genérica. **Causa raíz confirmada armando una base Postgres local desde cero** (Postgres 16 ya viene instalado en esta sesión): apliqué las migraciones existentes SIN la nueva de `DIARIO` (para replicar el estado real de la base de producción), reproduje el error EXACTO que reportó el usuario, apliqué la migración nueva, y confirmé que ahí sí funciona — o sea, el código está bien, la migración simplemente no se había corrido todavía.
- **Corrección importante a lo que se le dijo antes al usuario: correr la migración desde la consola del servicio `gym-app` en Easypanel NO iba a funcionar, y se descubrió recién en vivo (con el usuario pegando el error real de la consola).** El `Dockerfile` del proyecto genera una imagen final "standalone" de Next.js (`output: "standalone"` en `next.config.ts`) — el contenedor de producción NO tiene el repo completo: no tiene `packages/db/prisma/schema.prisma`, no tiene la carpeta `migrations/`, y ni siquiera tiene el Prisma CLI instalado (es una devDependency, nunca se importa en el código de la app en runtime, así que el tracing automático de Next.js no la arrastra a la imagen final). Por eso `npx prisma migrate deploy --schema=...` fallaba con "file or directory not found" — ese archivo literalmente no existe en ese contenedor. **Lección: antes de mandar a correr un comando dentro de la consola de un servicio en producción, revisar el Dockerfile/proceso de build real — no asumir que el contenedor final tiene el repo completo, sobre todo si el proyecto usa un build "standalone"/multi-stage pensado para ser liviano.**
- **Fix definitivo implementado y PROBADO localmente de punta a punta (sin acceso a Docker en esta sesión, pero con Postgres 16 real instalado):** se automatizaron las migraciones para que corran solas en cada arranque del contenedor, sin que nadie tenga que tocar ninguna consola nunca más. Archivos:
  - **`docker-entrypoint.sh`** (nuevo, en la raíz) — corre `prisma migrate deploy` (usando el binario ya instalado, sin descargar nada) y después `exec node apps/web-admin/server.js`. `set -e` así si la migración falla, el contenedor no arranca con una base desincronizada del código (falla visible en los logs de Easypanel, en vez de un bug silencioso).
  - **`Dockerfile`** — en la etapa `runner`, además del standalone de siempre, ahora también copia `node_modules` COMPLETO (no el recortado del tracing de Next.js) y `packages/db` completo a una carpeta aparte `./migrate-toolkit/` — **intento anterior descartado:** primero probé bajar el Prisma CLI con `npx` en cada arranque (sin copiar `node_modules` completo), pero falló porque el archivo de config del proyecto (`packages/db/prisma7.config.ts`) hace `import "prisma/config"` e `import "dotenv"`, paquetes que Node no puede resolver si no están en un `node_modules` real accesible desde esa ruta — confirmado reproduciendo el mismo error en una simulación local del layout del contenedor. Copiar el `node_modules` completo (más pesado, pero exactamente el árbol que ya resolvió `npm ci` sin sorpresas) fue lo que terminó funcionando. `CMD` se cambió a `ENTRYPOINT ["./docker-entrypoint.sh"]`.
  - **Verificación real hecha en esta sesión** (no solo revisión de código): armé una base Postgres 16 local, la dejé en el estado EXACTO de producción (16 migraciones aplicadas, falta la de `DIARIO`), copié el layout exacto que tendría el contenedor final (`node_modules` + `packages/db` + el script), y corrí `docker-entrypoint.sh` de punta a punta — aplicó la migración pendiente sola y arrancó "el server" simulado sin ningún error. Repetido también desde una base completamente vacía (las 17 migraciones desde cero) — mismo resultado.
  - **✅ VERIFICADO EN VIVO** — el usuario forzó la reconstrucción en Easypanel y después pudo crear el plan "Diario" y un miembro con ese plan sin ningún error, confirmando que las migraciones automáticas al arrancar el contenedor funcionan en producción real, no solo en la simulación local.
  - Mientras tanto, si el usuario quiere desbloquearse ANTES de ese rebuild, se le pasó una vía alternativa: correr `ALTER TYPE "FrecuenciaPago" ADD VALUE IF NOT EXISTS 'DIARIO';` directo contra la base (por el servicio de Postgres en Easypanel, no por la consola de `gym-app`) — bypassea a Prisma pero logra lo mismo de inmediato. **Ya se resolvió el posible choque con esto:** se le agregó `IF NOT EXISTS` al `ADD VALUE` del archivo real de la migración (`packages/db/prisma/migrations/20260924000000_frecuencia_diario/migration.sql`), así que da igual si el usuario ya corrió el SQL a mano o no — cuando el rebuild automático la aplique (por primera vez o de nuevo), no va a fallar por "el valor ya existe" en ningún caso. Nadie llegó a aplicar esta migración exitosamente contra producción todavía (el intento anterior falló antes de ejecutarla), así que no hay riesgo de checksum desincronizado por haber cambiado el archivo después de creado.
- **Roadmap del cliente, punto (b): membresías "cortesía" ($0) — se sacó la exigencia de elegir método de pago cuando el monto es $0**, en los tres lugares donde existía:
  - `apps/web-admin/app/(panel)/miembros/actions.ts` (`crearMiembroAction`, alta de miembro con "Primer pago"): `metodo`/`metodoPagoId` ahora solo son obligatorios si `precioPlan > 0`; si el monto es $0 y no se mandó `metodo`, se guarda como `"Cortesía"` (antes hubiera quedado como cadena vacía en el historial de pagos).
  - `apps/web-admin/app/(panel)/pagos/actions.ts` (`registrarPagoAction`): mismo criterio, `metodo`/`metodoPagoId` obligatorios solo si `monto > 0`, mismo fallback `"Cortesía"`.
  - `apps/web-admin/app/(panel)/miembros/FormularioMiembro.tsx` (card "Primer pago", alta de miembro), `apps/web-admin/app/(panel)/pagos/FormularioPago.tsx` (Registrar pago, usado en `/pagos/nuevo`, en la ficha del miembro, y en Caja vía `PanelPagoYCambioPlan`), y `apps/web-admin/app/(panel)/caja/ModalRegistrarPagoCaja.tsx` (paso de pago del wizard de Caja) — en los tres, cuando el monto es $0 ya NO se muestra la grilla de `SelectorMetodoPago`, se reemplaza por "Este plan no tiene costo — no hace falta elegir método de pago.", y el botón de submit correspondiente deja de depender de `seleccionMetodo.metodoPagoId` en ese caso.
  - No se tocó `CrearPlan.ts` ni el Server Action de planes — ya soportaban precio $0 sin cambios, como se había confirmado antes de implementar esto.
  - Verificado con un build completo de Next.js (`npm run build`, con `node_modules` instalado en esta sesión) sin errores de TypeScript. **Sin verificar todavía en vivo** — el usuario tiene que crear un plan "Cortesía $0" desde `/planes` (no hace falta ninguna migración esta vez, no toca la base) y probar el alta de un miembro con ese plan.
- **Roadmap del cliente, punto (c): entrenadores — alta simplificada sin acceso al sistema (solo nombre, teléfono, foto de perfil y sucursal).** No hizo falta tocar el esquema de la base — el enfoque fue generar credenciales internas inutilizables en vez de pedirlas:
  - `apps/web-admin/app/(panel)/usuarios/FormularioUsuario.tsx` (`FormularioUsuario`, creación): el `<select name="rol">` ahora es controlado (`useState`); cuando `rol === "ENTRENADOR"` los campos "Email" y "Contraseña" directamente no se renderizan (no solo se ocultan — así no interfieren con la validación `required` del navegador), con una nota explicando por qué. "Sucursales asignadas" se dejó tal cual (el mismo fieldset de checkboxes que ya existía) — el gerente marca una sola casilla para representar "la sucursal" del entrenador; no se creó un campo nuevo, ya cumplía el pedido.
  - `FormularioEditarUsuario` (edición): ya no tenía campos de email/password editables (nunca los tuvo), pero mostraba el email como texto informativo de solo lectura — para un entrenador eso mostraría el email placeholder interno, que no tiene sentido para el gerente. Ahora, si `rol === "ENTRENADOR"`, esa línea dice "Rol: ENTRENADOR (sin acceso al sistema)" en vez de mostrar el email.
  - `apps/web-admin/app/(panel)/usuarios/actions.ts` (`crearUsuarioAction`): cuando `rol === "ENTRENADOR"`, en vez de leer `email`/`password` del formulario, genera `email = \`entrenador-${randomUUID()}@sinacceso.interno\`` (único por el UUID, nadie lo conoce) y `password = randomUUID()` (se hashea con el mismo `BcryptPasswordHasher` de siempre y se descarta — nunca se guarda en texto plano ni se muestra en ningún lado). La cuenta sigue existiendo como `UsuarioAdmin` de verdad (el esquema lo exige), pero es 100% inutilizable para loguearse ya que nadie conoce ni el email ni la contraseña reales. Para cualquier otro rol, el comportamiento es exactamente el mismo que antes (pide email/password reales).
  - No se tocó `CrearUsuarioAdmin.ts` (el caso de uso de dominio) para nada — sigue recibiendo `email`/`passwordHash` como strings normales, sin saber que en este caso son generados; toda la lógica nueva vive en la Server Action, que es la capa correcta para esto.
  - Verificado con un build completo de Next.js sin errores de TypeScript. **Sin verificar todavía en vivo** — el usuario tiene que probar crear un entrenador desde `/usuarios/nuevo` y confirmar que no pide email/contraseña, que el entrenador queda creado igual, y que aparece correctamente donde ya se usa (selector de entrenador al asignar plan con entrenador a un miembro, tarjeta en `/usuarios`, etc.).
- **Ajuste visual en `/usuarios/nuevo` — campos se veían "muy largos y estirados"**, con mockup aprobado por el usuario ANTES de tocar código (`https://claude.ai/artifact/7k38YGuWqgonSbkoZwBfdF`). Causa: `nuevo/page.tsx` no tenía ningún `max-w-*` (usa todo el ancho del panel) y `FormularioUsuario.tsx` era un `<form>` suelto sin `Card` — cada `Input` se estiraba a lo ancho de toda la pantalla. `apps/web-admin/app/(panel)/usuarios/FormularioUsuario.tsx` (`FormularioUsuario`, creación): ahora el formulario vive dentro de un `<Card>` contenida en `max-w-md` (mismo patrón que ya usa `FormularioEditarUsuario`), con el label "Datos del usuario" arriba (igual que "Datos personales" en miembros); campos reordenados — Nombre y Teléfono cada uno en su propia fila (siempre visibles, no dependen del rol), Email+Contraseña en un grid de 2 columnas SOLO cuando `!esEntrenador` (agrupados así porque aparecen/desaparecen juntos — evita un grid con un hueco vacío si estuvieran agrupados con Teléfono, que siempre está); Rol y la nota de entrenador igual que antes; "Sucursales asignadas" pasó de una columna a un grid de 2 columnas para no alargar tanto la card. No se tocó `nuevo/page.tsx` — el `max-w-md` ya vive adentro de `FormularioUsuario`, no hizo falta nada en el contenedor de la página. No se tocó `FormularioEditarUsuario` (el usuario pidió el ajuste específicamente para "Nuevo usuario"; la edición ya tenía `max-w-md`, aunque sin `Card` — si se pide después, aplicar el mismo patrón ahí). Verificado con build completo sin errores. **Sin verificar todavía en vivo.**
- **Bug real encontrado al verificar en vivo el punto (b) (cortesía $0): al crear un miembro nuevo con el plan de $0, el navegador bloqueaba el "Guardar" con "ingresá un valor".** Causa raíz: `FormularioMiembro.tsx` tiene una validación de cliente separada de la del método de pago — `manejarClickGuardar()` (el handler del botón "Guardar" de la card "Datos personales", que valida TODO el formulario antes de abrir el ticket de confirmación) rechazaba cualquier precio `<= 0`, sin excepción. Esa validación es anterior al punto (b) y nunca se ajustó cuando se agregó el plan de cortesía. **Fix (opción B, elegida por el usuario sobre simplemente "permitir $0 en general"): el precio de un plan de cortesía queda BLOQUEADO, no editable.** Nueva variable derivada `planEsCortesia = planSeleccionado?.precioUSD === 0` (mira el precio OFICIAL del plan elegido en `/planes`, no el valor tipeado). Cuando `planEsCortesia` es true: (1) el campo "Precio" editable (`CurrencyInput` de `precioPlanEditado`, dentro del bloque `editandoPlan`) se reemplaza por un recuadro de solo texto "$0 — plan de cortesía (no editable)" — ya no se puede pisar el precio a mano; (2) la validación de `manejarClickGuardar` pasa a `if (!planEsCortesia && (!precio || Number(precio) <= 0))`, o sea $0 solo se acepta cuando el plan elegido es oficialmente gratis, cualquier otro plan sigue exigiendo precio > 0 igual que antes. El valor real que se manda al servidor sigue siendo el `<input type="hidden" name="precioPlan" value={precioActual} />` que ya existía (siempre montado, fuera del bloque condicional) — no hizo falta tocar el Server Action ni `RegistrarPago.ts`. Verificado con build completo sin errores de TypeScript. **✅ VERIFICADO EN VIVO por el usuario** — ya puede guardar un miembro nuevo con el plan de cortesía sin el error de "ingresá un valor".
- **Roadmap del cliente, punto (d): pagos fraccionados + pagos mixtos, unificados como "abonos".** Reglas de negocio confirmadas por el usuario (con mi interpretación, que aceptó): acceso inmediato desde el primer abono; el ciclo se considera saldado solo al llegar al 100% (esto es informativo — un "saldo pendiente" — no bloquea nada); cada abono en fila separada con etiqueta de a qué ciclo pertenece.
  - **`packages/domain/entities/Pago.ts`** — dos funciones puras nuevas, únicas responsables de "qué pagos pertenecen a este ciclo y cuánto suman" (las usa tanto la escritura como la lectura, para no duplicar el criterio):
    ```ts
    export function pagosVigentesDelCiclo(pagos: Pago[], fechaFinCiclo: Date): Pago[] {
      return pagos.filter((pago) => !pago.anuladoEn && pago.fechaFinCiclo?.getTime() === fechaFinCiclo.getTime());
    }
    export function totalPagado(pagos: Pago[]): number {
      return pagos.reduce((suma, pago) => suma + pago.monto, 0);
    }
    ```
  - **`packages/domain/use-cases/RegistrarPago.ts`** — el cambio central. Antes: CUALQUIER pago se trataba como el precio completo del plan y siempre extendía el vencimiento. Ahora: si ya hay una Suscripción vigente (`activa.fin > ahora`) Y la suma de los pagos no anulados que apuntan a `activa.fin` todavía no llega a `miembro.precioPlan` (el precio ACORDADO con ese miembro — no `Plan.precioUSD`, que es el precio de catálogo y puede diferir si se negoció un precio distinto), el pago nuevo es un "abono" que se suma al MISMO ciclo: no se llama `extenderFin` ni `actualizarFechasPago` de nuevo (el vencimiento ya se había adelantado en el primer abono — así es como queda habilitado el acceso desde el primer pago, sin tocar el check-in del kiosco para nada). Si no se cumple esa condición (no hay ciclo vigente, o el vigente ya está saldado, o es la primera vez), el comportamiento es el de siempre: se calcula `base`/`fin` y se extiende/crea la Suscripción. El `fechaInicioCiclo` de un abono que completa un ciclo existente se reutiliza del primer abono de ese mismo ciclo (todos comparten el mismo rango), no se recalcula.
  - **`apps/web-admin/app/(panel)/pagos/FormularioPago.tsx`** (usado en la ficha del miembro vía `PanelPagoYCambioPlan`) — el campo "Monto" del bloque de plan fijo, que antes era texto fijo + `<input type="hidden">` con el precio completo (no se podía pagar menos), ahora es un `CurrencyInput` editable, precargado con el saldo pendiente si hay un ciclo a medio pagar (o el precio completo si no) — nueva prop opcional `saldoPendiente`. Muestra una nota si el monto tipeado es menor al precio del plan ("queda como abono").
  - **`apps/web-admin/app/(panel)/miembros/PanelPagoYCambioPlan.tsx`** — nueva prop `saldoPendiente`, la pasa a `FormularioPago` y además muestra un banner propio ("Saldo pendiente de este ciclo: $X") arriba de las pestañas, visible sin importar cuál esté activa.
  - **`apps/web-admin/app/(panel)/miembros/[id]/page.tsx`** — calcula `saldoPendiente` con `pagosVigentesDelCiclo(pagos, miembro.fechaVencimiento)` + `totalPagado(...)` comparado contra `miembro.precioPlan` (ya tenía `pagos` cargado para "Ciclos", no hizo falta ninguna consulta nueva a la base), y se lo pasa a `PanelPagoYCambioPlan`.
  - **`apps/web-admin/app/(panel)/caja/ModalRegistrarPagoCaja.tsx`** (Paso 3 del wizard, `ContenidoPaso3`) — mismo cambio de fondo: el `monto` (antes fijo, hidden input) ahora es un `CurrencyInput` editable, precargado con el precio de lista del plan. **Limitación a propósito, por alcance:** acá NO se calcula el saldo pendiente real (para eso habría que traer el historial de pagos de CADA miembro al buscador de Caja, que hoy arma la lista en el cliente a partir de datos livianos) — quien cobra tiene que fijarse el saldo exacto en la ficha del miembro (`/miembros/{id}`) si es un abono, acá solo puede tipear cualquier monto.
  - **`apps/web-admin/app/(panel)/miembros/[id]/pagos/page.tsx`** (historial de pagos del miembro) — nueva función `etiquetaAbono(pago, todos)` que usa `pagosVigentesDelCiclo` para detectar si un pago comparte ciclo con otros (ordenados por fecha ascendente) y arma "Abono 1 de 2", etc. — solo aparece cuando hay más de un pago en el mismo ciclo. Se agregó al lado de la columna/línea "Ciclo" tanto en la tabla de escritorio como en las cards mobile. **No se tocó `/pagos` (Histórico de Pagos general)** — es un reporte de cierre de caja por turno, no por miembro, y agregar esta etiqueta ahí complicaría una tabla ya densa sin aportar tanto (la ficha del miembro es el lugar natural para revisar sus abonos) — si el usuario lo pide, es un agregado chico.
  - **No se tocó `crearMiembroAction`/`FormularioMiembro.tsx` (alta de un miembro nuevo)** — el "primer pago" al inscribir sigue exigiendo el monto completo de una sola vez; fraccionar aplica a partir del primer "Registrar pago" que se haga después del alta (incluida la primera renovación). Fraccionar el pago de inscripción en sí no se armó esta vez, para no duplicar el alcance — es un agregado chico si el cliente lo pide.
  - **Verificado de punta a punta contra una base Postgres 16 real** (no solo build) — un script armó una organización/sucursal/plan $30/miembro de prueba, llamó a `registrarPago` tres veces ($10 efectivo, $10 pago móvil, $10 tarjeta) simulando pagos fraccionados y mixtos, y confirmó: el vencimiento se adelanta en el primer abono (acceso inmediato) y NO se mueve de nuevo en el 2do ni el 3ro; sigue existiendo una sola `Suscripcion` (no se duplica); el saldo pendiente calculado a mitad de camino dio exactamente $10; al completar el ciclo, un pago siguiente sí abre un ciclo nuevo correctamente. También pasó el build completo de Next.js sin errores de TypeScript. **Sin verificar todavía en vivo por el usuario.**
- **Ronda de pruebas propia (a pedido del usuario: "haz un test tú mismo... dime si encuentras bugs") sobre los 4 puntos ya implementados del roadmap, contra una base Postgres real de punta a punta (no solo build/revisión de código).** Además de re-confirmar los casos básicos de los 4 puntos, se probaron casos límite nuevos: (a) el **check-in real del kiosco** (`registrarCheckIn`, no solo mirar `fechaVencimiento`) deja pasar tanto a un miembro de cortesía como a uno que solo pagó el primer abono parcial; (b) un **sobrepago** (abono que excede el saldo pendiente) no duplica el ciclo ni rompe nada; (c) **anular un abono** deja de contarlo para el saldo pendiente, y el abono siguiente se sigue sumando bien al mismo ciclo; (d) **"Cambiar de plan" (cobrar diferencia) mientras hay un ciclo a medio pagar** — el caso más delicado, probado subiendo de un plan de $30 (con $15 ya abonados) a uno de $40: cobra los $10 de diferencia correctamente y el saldo pendiente resultante ($15) es matemáticamente correcto, no se pierde ni se duplica plata en el cálculo. **Todos estos casos pasaron sin problema.**
- **Hallazgo real de esta ronda (y su fix): un pago de $0 contra un plan PAGO (no de cortesía) adelantaba el vencimiento igual, dando acceso gratis sin cobrar nada.** No era un bug nuevo de esta sesión (el dominio nunca validó que `monto` fuera coherente con el precio del plan), pero se volvió más fácil de disparar por accidente porque el campo "Monto" ahora queda editable en más pantallas (antes, en varios flujos, venía fijo/bloqueado al precio del plan). **Fix:** `packages/domain/use-cases/RegistrarPago.ts` — nuevo `MontoInvalidoError` y chequeo `if (miembro.precioPlan > 0 && input.monto <= 0) throw new MontoInvalidoError()`, agregado justo después de validar la sucursal del miembro (antes de tocar Suscripción/Pago). Usa `miembro.precioPlan` (el precio ACORDADO con ese miembro, no `Plan.precioUSD`) — coherente con el resto de la lógica de abonos. Un plan de cortesía real (`precioPlan === 0`) sigue aceptando $0 sin problema, no se tocó ese caso. Se agregó el manejo del nuevo error (`return { error: error.message }` en vez de un 500 sin capturar) en los dos Server Actions que llaman a `registrarPago`: `apps/web-admin/app/(panel)/pagos/actions.ts` (`registrarPagoAction`) y `apps/web-admin/app/(panel)/miembros/actions.ts` (`crearMiembroAction`, aunque ahí es inalcanzable en la práctica porque `monto` y `Miembro.precioPlan` vienen del mismo valor — se agregó igual por defensa). **Verificado con un segundo script contra una base real:** un $0 contra un plan de $30 ahora se rechaza con el mensaje "El monto tiene que ser mayor a $0 — este plan no es de cortesía" (y el vencimiento no se mueve), un monto negativo también se rechaza, un abono parcial legítimo ($15) sigue funcionando normal, y el plan de cortesía real sigue aceptando $0 sin ningún cambio de comportamiento. Build completo sin errores de TypeScript.

- **Etapa 1 — tasa BCV automática, 2026-09-26 (EN PROGRESO, sesión actual):** implementación de `docs/superpowers/plans/2026-09-26-tasa-bcv-automatica.md`, Etapa 1 (regla de vigencia + reconciliación, sin disparo automático todavía — eso es Etapa 2). Criterio de negocio: siempre se cobra con la última tasa BCV publicada, desde el momento de su publicación (nunca se difiere por fecha valor). Trabajado directo en `main`, sin rama nueva (decisión explícita del usuario). El intervalo de refresco perezoso de Etapas 2-3 se fijó en **45 min** (no 5 min como decía el borrador original del plan) — decisión explícita del usuario al iniciar esta sesión, ya corregida en el propio archivo del plan.
  - `packages/domain/utils/fechaCaracas.ts` (nuevo) — `diaCalendarioCaracas`/`sumarDias`, día calendario en America/Caracas sin depender de `TZ` del proceso.
  - `packages/domain/ports/IExchangeRateService.ts` — reemplaza `obtenerTasaOficial()` por `obtenerPublicadas(versionConocida)` devolviendo `SIN_CAMBIOS` o `CAMBIOS` con ETag y el array de tasas.
  - `packages/domain/ports/ITasaCambioRepository.ts` — agrega `guardarVarias(tasas, fuente)` (no quita `guardar`/`obtenerUltima`).
  - `packages/domain/use-cases/ObtenerTasaVigente.ts` (nuevo, reemplaza a `ObtenerTasaActual.ts`, eliminado) — siempre la última tasa guardada, con `estado: "AL_DIA" | "DESACTUALIZADA"` (desactualizada si su fecha valor es anterior a "hoy" en Caracas).
  - `packages/domain/use-cases/SincronizarTasas.ts` (nuevo, reemplaza a `ActualizarTasaDiaria.ts`, eliminado) — reconciliación: ventana de 7 días antes de la última fila guardada (60 días si la tabla está vacía), filtra filas inválidas, upsert idempotente por fecha, propaga errores de red sin inventar filas.
  - `packages/infrastructure/exchange-rate/BcvApiAdapter.ts` — ahora consume el endpoint de **históricos** de DolarAPI (`/v1/historicos/dolares/oficial`) con `If-None-Match`/ETag (`304` = sin cambios), descarta filas con fecha/promedio inválidos sin lanzar, stateless (no guarda el ETag).
  - `packages/infrastructure/persistence/prisma/PrismaTasaCambioRepository.ts` — nuevo `guardarVarias` (upsert en `$transaction`, misma normalización de fecha que `guardar`).
  - `apps/worker/src/actualizar-tasa.ts` — reescrito para llamar `sincronizarTasas` + `obtenerTasaVigente` (sigue siendo el mecanismo manual de respaldo, `npm run actualizar-tasa`); mensajes de consola actualizados (`✅ Sincronizado: N filas...` / `⚠️ DolarAPI falló...`).
  - `apps/web-admin/app/api/tasa-cambio/route.ts` — usa `obtenerTasaVigente`, responde `{ ...tasa, estado }` (cambio aditivo, los consumidores solo leen `.valor`).
  - `apps/web-admin/app/(panel)/caja/page.tsx` — mismo reemplazo (`obtenerTasaVigente` en vez de `obtenerTasaActual`), conserva el `.catch` de `SinTasaDisponibleError` → `null`.
  - `packages/domain/entities/MetodoPago.ts` — comentario actualizado (`ObtenerTasaActual` → `ObtenerTasaVigente`).
  - `apps/web-admin/scripts/verificar-tasa-bcv.ts` (nuevo, mismo patrón que `verificar-autorizacion.ts`) — 11 casos con `node:assert/strict`, repositorio en memoria y servicio falso, cubriendo `obtenerTasaVigente` (AL_DIA/DESACTUALIZADA, tabla vacía) y `sincronizarTasas` (ventana de reconciliación, `SIN_CAMBIOS`, propagación de errores, filas inválidas descartadas). `npx tsx scripts/verificar-tasa-bcv.ts` desde `apps/web-admin` → **OK (11/11)**.
  - **Builds:** `apps/worker` → `npx tsc --noEmit` limpio (exit 0). `apps/web-admin` → `npx tsc --noEmit`, `npx turbo run build --filter=web-admin` y `npx turbo run lint --filter=web-admin` fallan, pero **por causas preexistentes en `main`, confirmadas con `git stash`/`git stash pop` (no introducidas por este plan)**: dos errores de tipo en `packages/infrastructure/persistence/prisma/PrismaPlanRepository.ts` (líneas 54 y 89, `FrecuenciaPago "DIARIO"` no asignable al enum generado de Prisma — probablemente el cliente Prisma generado quedó desactualizado tras la migración de `DIARIO` de una sesión anterior) y 6 errores/11 warnings de lint en archivos que esta sesión no tocó (`FormularioMiembro.tsx`, `SelectorMetodoPago.tsx`, `layout.tsx`, otros). **No se tocaron** por ser fuera del alcance de este plan.
  - `TASA_BCV_FIJA` (`apps/web-admin/app/(panel)/tasaBcvFija.ts`) — revisado en el pre-flight: confirmado que NO se usa en ningún cálculo de dinero real (solo se importan `formatearBs`/`formatearBsConRef` de ese archivo en 7 lugares). No se tocó, según indica el plan.
  - Pendiente de esta etapa: Tarea 1.11 (prueba real con red + base de datos — la ejecuta el usuario: `npm run actualizar-tasa` dos veces + `curl` a `/api/tasa-cambio`).
  - **Commits de Etapa 1 ya aplicados en `main` (sin push):** `51f35be` (última tasa publicada), `70da583` (sincronización por reconciliación), `3a3cf86` (script de verificación), `14ac492` (docs: plan + handoff) — a pedido del usuario, cada commit sugerido del plan se armó por separado con su propio `git add` selectivo, más un 4to commit aparte para `handoff.md`/el plan (no forma parte de los 3 sugeridos por el plan original).

- **Etapa 2 — tasa BCV automática, 2026-09-26 (EN PROGRESO, misma sesión): refresco perezoso durante el uso del panel.** Sigue en `main`, sin commitear todavía.
  - `apps/web-admin/lib/tasaBcv.ts` (nuevo) — `crearOrquestadorTasa` (lógica pura: throttle de **45 min**, single-flight con `enCurso`, timeout bloqueante de 2.5s solo si la tasa está `DESACTUALIZADA` o se pide `forzar: true`, si no dispara en segundo plano vía `programar`) + instancia singleton `orquestadorTasa` cableada con `PrismaTasaCambioRepository`/`BcvApiAdapter`/`after()` de `next/server`. Maneja la tabla vacía: si `obtenerVigente` lanza `SinTasaDisponibleError`, sincroniza bloqueante una vez y reintenta la lectura antes de propagar.
  - `apps/web-admin/app/api/tasa-cambio/route.ts` y `apps/web-admin/app/(panel)/caja/page.tsx` — migrados de `obtenerTasaVigente` directo a `orquestadorTasa.obtenerTasaVigenteFresca()` (dispara la verificación en cada request, con throttle).
  - `packages/ui/components/RelojYTasa.tsx` — vuelve a pedir `/api/tasa-cambio` cada 45 min mientras la pestaña está visible, y también al volver de `visibilitychange`.
  - `apps/web-admin/app/(panel)/pagos/SelectorMetodoPago.tsx` y `apps/web-admin/app/(panel)/caja/ModalRegistrarEgreso.tsx` — nuevo estado `cargadaEnMs`; si el modal queda abierto con un método en Bs elegido y pasan >=45 min desde la última carga, revalida la tasa y actualiza el monto en Bs mostrado sin cerrar el modal.
  - `apps/web-admin/scripts/verificar-tasa-bcv.ts` — extendido con los casos 12-17 (throttle, DESACTUALIZADA, timeout de sincronización lenta, single-flight con 5 llamadas simultáneas, fallo de sincronización no actualiza la versión, `forzar: true` ignora el throttle). `npx tsx scripts/verificar-tasa-bcv.ts` → **OK (17/17)**.
  - **Builds:** mismos resultados que Etapa 1 — `apps/worker` limpio; `apps/web-admin` con los mismos 2 errores de build y 17 problemas de lint (6 errores/11 warnings) **preexistentes**, confirmado sin diferencias frente al baseline de Etapa 1 (nada nuevo introducido).
  - Pendiente de esta etapa: verificación manual con `npm run dev` (confirmar en terminal un log `[tasa-bcv]` como máximo cada 45 min) — la hace el usuario.
  - **Commits de Etapa 2 ya aplicados en `main` (sin push):** `3dc6226` (feature), `15bce36` (docs handoff). A partir de esta etapa, a pedido del usuario, cada commit se arma agregando los archivos al stage **uno por uno** (nunca varias rutas en un solo `git add`).

- **Etapa 3 — tasa BCV automática, 2026-09-26 (EN PROGRESO, misma sesión): protección de cobros, nunca registrar con una tasa vieja.** Sigue en `main`, sin commitear todavía.
  - **Ampliación pedida por el usuario ANTES de implementar (confirmada con preguntas explícitas):** además de "la tasa cambió, reconfirma" (ya definido en el plan original), se agregó un segundo caso — **DolarAPI falla justo al forzar la verificación en el momento de cobrar** (fallo temporal, no un cambio de tasa). Decisión: (a) el aviso aparece **solo al momento de cobrar**, no en el badge `RelojYTasa` ni otras pantallas; (b) ese caso también **bloquea** el registro (igual de estricto que un cambio de tasa) y muestra un aviso con 3 opciones — usar la última guardada igual, abrir `https://bcv.org.ve/` en pestaña nueva, o ingresar el valor manualmente (reusa el flujo ya existente de `ModalIngresoManualTasa`/`registrarTasaManualAction`, fuente `"MANUAL"`, acepta valores como `857,00580000`). El plan (`docs/superpowers/plans/2026-09-26-tasa-bcv-automatica.md`, sección Etapa 3) se actualizó con esta ampliación antes de tocar código.
  - `apps/web-admin/lib/tasaBcv.ts` — `crearOrquestadorTasa` ahora devuelve `TasaVigenteFresca` (agrega `sincronizacionFallida: boolean`, campo aditivo, no rompe los consumidores de Etapa 2). Nuevo helper puro `validarTasaCobro(tasaFormulario, fresca)`: compara con tolerancia de redondeo de 4 decimales y devuelve `{ok:true, tasa}` / `{ok:false, error, tasaNueva}` / `{ok:false, error, fallaTemporal:true, tasaGuardada}`.
  - `apps/web-admin/app/(panel)/pagos/actions.ts` (`registrarPagoAction`) y `apps/web-admin/app/(panel)/caja/actions.ts` (`registrarEgresoAction`) — cuando la operación es en Bs, llaman `orquestadorTasa.obtenerTasaVigenteFresca({forzar:true})` + `validarTasaCobro` antes de registrar; nunca usan la tasa del formulario, siempre la del servidor tras validar. `EstadoFormularioPago`/`EstadoRegistrarEgreso` extendidos con `tasaNueva?`/`fallaTemporal?`/`tasaGuardada?`.
  - **Hallazgo corregido (a pedido del usuario, mismo día):** `cambiarPlanAction` (mismo archivo `pagos/actions.ts`) también recibía `tasaCambio` del cliente sin validar — mismo riesgo que los otros dos flujos. Se extrajo `EstadoErrorTasa` (forma compartida `{error, tasaNueva?, fallaTemporal?, tasaGuardada?}`) para que `validarTasaSiEsEnBs` sirva tanto a `registrarPagoAction` como a `cambiarPlanAction` sin duplicar lógica. `EstadoCambioPlan` extendido con los mismos 3 campos opcionales; `FormularioCambiarPlan.tsx` pasa `avisoServidor` a `SelectorMetodoPago` igual que `FormularioPago.tsx`. Verificado: `tsc --noEmit` solo con los 2 errores preexistentes, script `OK (22/22)`, `turbo lint --filter=web-admin` sin cambios (19 problemas, idéntico al resto de la Etapa 3 — no se introdujo nada nuevo al reusar el patrón ya cableado).
  - `apps/web-admin/app/(panel)/pagos/SelectorMetodoPago.tsx` — nueva prop `avisoServidor`; nuevo componente `ModalFallaTemporalTasa` (3 acciones); `ModalIngresoManualTasa` y `ModalFallaTemporalTasa` exportados (antes privados) para reusarlos en `ModalRegistrarEgreso.tsx` sin duplicar el flujo. `FormularioPago.tsx` y `ModalRegistrarPagoCaja.tsx` pasan `avisoServidor` desde el `estado` de la Server Action. `ModalRegistrarEgreso.tsx` — mismo patrón, con sus propios modales (no usa `SelectorMetodoPago`).
  - `apps/web-admin/scripts/verificar-tasa-bcv.ts` — extendido con el caso 18 (`sincronizacionFallida` al forzar con el servicio fallando) y los casos 19-22 (`validarTasaCobro`: coincide, tolerancia de redondeo, difiere, falla temporal). `npx tsx scripts/verificar-tasa-bcv.ts` → **OK (22/22)**.
  - **Builds:** `tsc --noEmit` (worker/web-admin) y `turbo build --filter=web-admin` → mismos bloqueos preexistentes de siempre, nada nuevo. `turbo lint --filter=web-admin` → confirmado con `git stash` que el baseline real es 17 problemas (6 errores/11 warnings); con estos cambios queda en 19 (8 errores/11 warnings) — **2 errores nuevos genuinos**, ambos `react-hooks/set-state-in-effect` en los `useEffect` que sincronizan el estado local con la respuesta de la Server Action (`SelectorMetodoPago.tsx`, `ModalRegistrarEgreso.tsx`). Esa misma regla ya estaba incumplida sin corregir en 2 lugares preexistentes del proyecto antes de esta sesión y no hay ningún patrón de `eslint-disable` para ella — se dejó igual, sin suprimir ni refactorizar, por ser exactamente la misma forma de deuda técnica ya tolerada. 0 warnings nuevos.
  - Pendiente de esta etapa: Tarea 3.3 verificación manual (cambiar la tasa guardada en desarrollo con el script + un servicio falso, y confirmar en el cobro real tanto el aviso de "tasa cambió" como el aviso de "falla temporal") — la hace el usuario.

- **Historial de tasa BCV en el badge, 2026-09-26 (misma sesión, plan aparte: `docs/superpowers/plans/2026-09-26-historial-tasa-bcv.md`).** Al pulsar el texto de la tasa en `RelojYTasa` se abre un modal con el historial completo de `TasaCambio`. Diseñado con `/grill-me` — decisiones clave: (1) DolarAPI **no expone la hora real de publicación del BCV** (verificado en vivo contra los 2 endpoints el 2026-09-26), así que las filas automáticas solo muestran fecha valor, sin hora; (2) para tasas MANUALES sí se conoce la hora exacta del registro (`createdAt`) y ahora también quién la registró — se agregó `TasaCambio.registradoPorId` (nullable, migración nueva); (3) "Últimas 20 + Cargar más" (botón, sin scroll infinito automático) y un modo aparte "Buscar por fecha exacta" con fallback a "la más cercana anterior" si no hay registro justo esa fecha; (4) columna "Registrado por" con formato `AUTO: DolarAPI` / `Nombre (2:32 p.m.)` / `Usuario no registrado (2:32 p.m.)` para MANUAL sin `registradoPorId` (filas previas a esta migración); (5) sin restricción de permisos — dato público; (6) responsive, mismo patrón `lg:hidden`/`hidden lg:block` que `ListaMiembros.tsx`.
  - **Migración:** `packages/db/prisma/migrations/20260926120000_tasa_cambio_registrado_por/migration.sql` — `ALTER TABLE "TasaCambio" ADD COLUMN "registradoPorId" TEXT;`. Corrida localmente contra el cliente Prisma generado (`npx prisma generate`), no contra ninguna base real en esta sesión.
  - **Hallazgo importante, no relacionado a esta feature pero descubierto y resuelto de paso:** correr `npx prisma generate` (necesario para que el cliente reconociera `registradoPorId`) también resolvió los 2 errores de TypeScript preexistentes en `packages/infrastructure/persistence/prisma/PrismaPlanRepository.ts` (`FrecuenciaPago`/`"DIARIO"` no asignable al enum generado) que venían arrastrándose desde la Etapa 1 de tasa BCV automática — confirma la sospecha que ya estaba anotada más arriba en este mismo `handoff.md`. **`tsc --noEmit` y `npx turbo run build --filter=web-admin` en `web-admin` quedan 100% limpios por primera vez en toda esta serie de sesiones.** Si en algún despliegue futuro reaparece ese error, correr `npx prisma generate` desde `packages/db` antes de investigar nada más.
  - `packages/domain/entities/TasaCambio.ts` — nuevos campos `registradoPorId: string | null` y `createdAt: Date`.
  - `packages/domain/ports/ITasaCambioRepository.ts` — `guardar` acepta `registradoPorId` opcional (no rompe callers viejos); nuevos métodos `listarHistorico`, `buscarPorFecha`, `buscarMasCercanaAnterior`.
  - `packages/domain/use-cases/RegistrarTasaManual.ts` — `registradoPorId` ahora es **obligatorio** en el input: cualquier pantalla futura que registre una tasa manual queda forzada a pasar el usuario de su propia sesión (a pedido explícito del usuario, para que el mecanismo sea reciclable sin duplicar lógica).
  - `packages/domain/use-cases/ListarHistoricoTasas.ts` (nuevo) — orquesta los 2 modos: `{modo:"recientes", antesDe, limite}` (paginación cursor-based, pide `limite+1` para saber si hay más sin una segunda consulta) y `{modo:"fecha", fecha}` (busca exacta, si no existe cae a `buscarMasCercanaAnterior`).
  - `packages/infrastructure/persistence/prisma/PrismaTasaCambioRepository.ts` — implementa los 3 métodos nuevos del puerto; `guardar`/`mapear` persisten y devuelven `registradoPorId`/`createdAt`.
  - `apps/web-admin/app/(panel)/configuraciones/actions.ts` (`registrarTasaManualAction`) — ahora destructura `usuario` de la sesión y pasa `usuario.id` como `registradoPorId`.
  - `apps/web-admin/app/api/tasa-cambio/historial/route.ts` (nuevo) — `GET` con dos formas: `?limite=&antesDe=` (paginado) y `?fecha=YYYY-MM-DD` (búsqueda exacta). Resuelve los nombres de `UsuarioAdmin` en una sola consulta por página (`findMany` con `id: { in: [...] }`, sin N+1). `401` sin sesión.
  - `apps/web-admin/app/(panel)/ModalHistorialTasas.tsx` (nuevo) — el modal en sí: pestañas "Últimas"/"Buscar por fecha", tabla en desktop, cards en mobile, botón "Cargar más", aviso + atajo "Ver la más cercana anterior" cuando no hay tasa exacta para la fecha buscada.
  - `packages/ui/components/RelojYTasa.tsx` — el texto de la tasa (`Bs. X (BCV)`) ahora es un `<button>` con `onClick={onClickTasa}` opcional (prop nueva), `cursor-pointer` + `hover:opacity-70`; el reloj y el separador `|` no son clickeables. Diseño visual sin cambios.
  - `apps/web-admin/app/(panel)/RelojYTasaConHistorial.tsx` (nuevo) — wrapper cliente que junta `RelojYTasa` + `ModalHistorialTasas` con su propio `useState`, porque `layout.tsx` (donde se monta) es un server component y no puede manejar el estado del modal directamente.
  - `apps/web-admin/app/(panel)/layout.tsx` — usa `RelojYTasaConHistorial` en vez de `RelojYTasa` directo.
  - `apps/web-admin/scripts/verificar-historial-tasa-bcv.ts` (nuevo, mismo patrón que `verificar-tasa-bcv.ts`) — 7 casos: `registrarTasaManual` con/sin `registradoPorId` válido, y los 5 casos de `listarHistoricoTasas` (primera página, página siguiente que agota la lista, búsqueda exacta, búsqueda sin exacta con/sin fallback). `OK (7/7)`.
  - `apps/web-admin/scripts/verificar-tasa-bcv.ts` (Etapa 1-3, ajeno a esta feature) — tuvo que actualizarse porque su propio `RepositorioEnMemoria` fake implementa `ITasaCambioRepository` completa: ganó los mismos 3 métodos nuevos del puerto y los 2 campos nuevos de la entidad, sin cambiar el comportamiento de sus 22 casos existentes. Sigue en `OK (22/22)`.
  - **Builds:** `tsc --noEmit` en `web-admin` → **0 errores** (ver el hallazgo de `prisma generate` arriba). `npx turbo run build --filter=web-admin` → **exit 0, build completo exitoso** (primera vez limpio en esta serie de sesiones). `npx turbo run lint --filter=web-admin` → 20 problemas (9 errores/11 warnings), **1 error nuevo genuino**: `react-hooks/set-state-in-effect` en `ModalHistorialTasas.tsx:106` (el `useEffect` que carga la primera página al montar/cambiar de modo) — misma naturaleza que los 2 casos ya tolerados como deuda técnica desde Etapa 3, no se suprime ni se refactoriza por consistencia.
  - **Revisión final (self-review, ejecución nativa sin subagente):** revisado el diff completo de la feature (17 archivos). Un hallazgo Important corregido: el botón "Ver la más cercana anterior" (modo búsqueda por fecha) actualizaba la fila mostrada pero no el `<input type="date">`, dejando el selector con la fecha original buscada mientras la tabla mostraba otra distinta — confuso para el usuario. Fix: el selector ahora se sincroniza con la fecha real de la tasa encontrada. Un hallazgo Minor diferido, sin corregir: la ruta trata `?limite=0` explícito como ausente (cae al valor por defecto de 20) por el operador `||` en JS — sin consumidor actual que lo dispare, es una trampa solo para un futuro llamador directo de la ruta.
  - Pendiente: verificación manual (la hace el usuario) — correr la migración contra una base de desarrollo real, abrir el panel, pulsar la tasa, confirmar "Cargar más", registrar una tasa manual y verla en el historial con el nombre y hora correctos, buscar una fecha sin tasa y confirmar el aviso + atajo (ahora también que el selector queda sincronizado), y probar en mobile que se ve como cards.

## 4. Intentos fallidos
- **Chrome DevTools MCP (`click`, `fill`, `press_key`) no simula bien eventos de teclado en inputs controlados de React en esta sesión** — el foco no quedaba realmente puesto (`document.activeElement` seguía en `<body>`), generando confusión sobre si `CurrencyInput` estaba roto cuando en realidad era el tooling de test. Se confirmó el fix real inyectando el valor vía `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set` + `dispatchEvent(new Event('input', {bubbles:true}))`. **Lección: verificar `document.activeElement` antes de asumir que un input está roto; no usar Chrome DevTools MCP salvo pedido explícito (ya en `CLAUDE.md`).**
- **Probar un fix de Server Action sin reiniciar el servidor de desarrollo tras `git pull`** llevó a pensar que el fix de `cerrarTurnoAction` no funcionaba (seguía guardando arqueo vacío) — Next.js/Turbopack no siempre recompila Server Actions en caliente cuando cambia qué campos de `FormData` lee. **Lección: reiniciar el servidor (parar y volver a levantar) después de cualquier pull que toque una Server Action.**
- **Un script de verificación propio apuntó mal a qué inputs eran "Monto contado"** al simular un cierre de turno vía JS, dejando un turno de prueba con arqueo vacío (no un bug real de la app). Ya limpiado de la base.
- **Intento de desplegar el daemon de tasa BCV en Easypanel se frenó por desconocer cómo `gym-app` recibe su `.env` cifrado (dotenvx)** — no hay campo de "Variables de entorno" visible en las acciones del servicio, y la variable a nivel de proyecto está vacía/bloqueada por licencia. **Lección: antes de crear un servicio nuevo que dependa de las mismas credenciales, confirmar primero DÓNDE y CÓMO se inyectan esas credenciales en el servicio existente — no asumir que Easypanel usa variables de entorno simples si los logs mencionan una herramienta como dotenvx.**
- **Compartir credenciales reales (DATABASE_URL, claves R2) en texto plano por chat** — pasó en esta sesión porque no se dejó claro con suficiente anticipación que había alternativas (pegarlas directo en Easypanel). Ya están expuestas en el historial de esta conversación. **Lección: la próxima vez que se necesite una credencial real, pedir explícitamente que se cargue directo en el campo de destino, no que se pegue en el chat.**
- **`npx turbo run build --filter=web-admin` y `npx turbo run lint --filter=web-admin` ya estaban rotos ANTES de esta sesión (Etapa 1 de tasa BCV), no es un bug introducido por este plan** — confirmado corriendo `git stash` (para quitar todos los cambios de esta sesión) y viendo que los mismos errores persisten: dos `TS2322` en `packages/infrastructure/persistence/prisma/PrismaPlanRepository.ts` (líneas 54 y 89) sobre `FrecuenciaPago "DIARIO"` no asignable al enum generado de Prisma, y 6 errores/11 warnings de ESLint en archivos no relacionados (`FormularioMiembro.tsx`, `SelectorMetodoPago.tsx`, `layout.tsx`). **Lección: antes de asumir que un build roto lo causó el cambio actual, correr `git stash` y repetir el comando para aislar la causa** — evitó perder tiempo arreglando algo fuera de alcance. Sospecha (sin confirmar): el cliente Prisma generado (`packages/db/generated/prisma`, gitignored) quedó desactualizado respecto al enum `FrecuenciaPago` tras la migración de "DIARIO" de una sesión anterior — probablemente se resuelve con `npx prisma generate` desde `packages/db`, pero no se intentó en esta sesión por no ser parte del alcance del plan de tasa BCV.
- **Reorganización de "Editar miembro" en dos columnas (Datos personales | Plan de membresía + panel de pago + Ciclos), REVERTIDA — rompía "Guardar".** Se movió la card "Plan de membresía" fuera del `<form id="formulario-miembro">` (a la columna derecha), usando el atributo `form={idFormulario}` en sus campos (`sucursalId` oculto, `entrenadorId` oculto/`<select>`) para que igual se enviaran al formulario principal — el mismo patrón que ya se usaba para el selector de método de pago en modo alta. El problema: esa misma card quedaba DENTRO del bloque `{!mostrarTicket && (...)}`, así que al mostrarse el ticket de confirmación ("Resumen de la edición") esos campos se desmontaban del DOM — y clickear "Sí, guardar" en ese momento mandaba el formulario SIN `sucursalId` (y sin `entrenadorId`), y el servidor devolvía el error genérico "Nombre, fecha de inscripción, sede y precio del plan son requeridos" (mensaje fijo que lista los 4 campos aunque solo falte uno). Confirmado por el usuario con capturas de pantalla, ya en producción — **revertido con `git revert` (commit `c970899`) y repusheado a `main` de inmediato** para no dejar el bug en vivo. **Lección: el patrón `form={idFormulario}` para sacar campos fuera del árbol DOM del `<form>` solo es seguro si esos campos permanecen SIEMPRE montados mientras el formulario pueda enviarse — no ponerlos dentro de un bloque condicional que pueda desmontarse en el momento del submit (como el overlay de ticket, que se muestra justo antes de enviar). Antes de repetir esta reorganización, pensar en otro enfoque: por ejemplo, mover los hidden inputs de sede/entrenador a un lugar SIEMPRE montado (igual que `planId`/`precioPlan`/`planNombre`, que si están arriba del todo en el `<form>` y por eso no se rompieron), y dejar que solo la presentación VISUAL de "Plan de membresía" se mueva a la derecha con CSS (order/grid-area) en vez de mover el nodo del DOM fuera del `<form>`.**

## 5. Próximos pasos

### 5.1. ROADMAP DEL CLIENTE (post-demo, PRIORIDAD ALTA — bloquea ir a producción)
El usuario le mostró el demo al cliente y volvió con 5 pedidos concretos antes de montar el sistema en el gym real. Se analizó cada uno contra el código actual (sin tocar nada todavía, solo lectura) y se propuso este orden — de menor a mayor riesgo/tamaño, dejando lo más grande e incierto para el final:

**a. Plan "Diario" en frecuencia de pago (`/configuraciones/planes`). ✅ CERRADO — verificado en vivo por el usuario.** Ver el detalle completo en la sección 3 ("Archivos y cambios"). Resumen: `FrecuenciaPago` ahora incluye `"DIARIO"` (1 día); tuvo un round de un bug real (falta la migración en producción, causa raíz mal diagnosticada al principio — pensé que corría desde la consola del contenedor de la app, pero esa imagen no tiene el repo completo) que terminó en automatizar las migraciones en el arranque del contenedor (`Dockerfile` + `docker-entrypoint.sh`, ver sección 3). El usuario creó un plan "Diario" a $3 y un miembro con ese plan sin errores.

**b. Membresías "cortesía" (con beneficios, sin cobro). ✅ CERRADO.** El usuario confirmó la interpretación: sí necesitan que se les lleve el registro completo (ciclo/vencimiento) con un plan a $0, no se puede omitir el registro. Como ya se sabía, el plan a $0 en sí ya funcionaba sin cambios de código — el usuario lo crea desde `/planes` con precio $0 (igual que hizo con el Diario). Lo que SÍ se arregló ahora fue la fricción real que se había detectado: el sistema exigía elegir un método de pago aunque el monto fuera $0, en TRES lugares (alta de miembro con plan gratis, "Registrar pago" normal, y el modal de pago en Caja) — ahora el método de pago solo es obligatorio cuando `monto > 0`; con $0 se guarda automáticamente como `metodo: "Cortesía"` en el historial (en vez de dejarlo vacío), y la grilla de métodos de pago ni se muestra, reemplazada por "Este plan no tiene costo — no hace falta elegir método de pago." Ver detalle completo en la sección 3.

**c. Entrenadores: alta simplificada, sin acceso al sistema (solo nombre, teléfono, foto de perfil y sucursal). ✅ IMPLEMENTADO — falta verificar en vivo (ver sección 3).** Extiende el trabajo que ya se hizo esta sesión (entrenadores sin permisos en el panel). El obstáculo real: en la base, `UsuarioAdmin.email` y `UsuarioAdmin.passwordHash` son campos obligatorios (`String @unique` / `String`, sin `?`) — confirmado en `schema.prisma` — así que técnicamente un entrenador sigue siendo un `UsuarioAdmin` y necesita ALGÚN valor ahí. **Recomendación (evita migración de base):** cuando el rol elegido en el formulario de "Nuevo usuario" sea ENTRENADOR, el formulario deja de pedir email/contraseña — el Server Action genera internamente un email placeholder único (nunca mostrado ni usable, ej. `entrenador-<id>@sinacceso.interno`) y un hash de una contraseña aleatoria descartada en el momento — así el entrenador queda con una cuenta técnicamente existente pero 100% inutilizable para loguearse, sin migrar el esquema. Alternativa más "limpia" pero más grande: hacer `email`/`passwordHash` nullable + una columna `sinAcceso` explícita, y bloquear el login para esos casos — más trabajo, solo vale la pena si el cliente pide algo más adelante que dependa de eso explícitamente.

**d. Pagos fraccionados (point 4) + pagos mixtos (point 5), unificados como "abonos". ✅ IMPLEMENTADO — falta verificar en vivo (ver sección 3).** El usuario confirmó las 3 reglas de negocio (con mi interpretación/recomendación, que aceptó tal cual):
1. El acceso al gym se habilita desde el PRIMER abono (no hay que esperar a completar el 100%).
2. El ciclo se considera "saldado" recién cuando la suma de abonos llega al precio acordado con el miembro — esto es solo informativo (saldo pendiente), no bloquea el acceso.
3. Cada abono queda como una fila separada en el historial de pagos, con una etiqueta que identifica a qué ciclo pertenece ("Abono 1 de 2", etc.).

Ver el detalle técnico completo en la sección 3. **Verificado de punta a punta contra una base Postgres real** (no solo build) — simulé 3 abonos de $10 sobre un plan de $30 con distintos métodos (efectivo, pago móvil, tarjeta) y confirmé: el vencimiento se adelanta en el primer abono y NO se vuelve a mover en el 2do/3ro, no se duplica la Suscripción, el saldo pendiente calcula bien en cada paso, y un pago posterior (ya saldado el ciclo) sí abre un ciclo nuevo correctamente.

**Nada de este roadmap quedó sin implementar** — los 4 puntos (a, b, c, d) del pedido del cliente están hechos. Lo que falta es pura verificación en vivo (ver sección 5.2, puntos 18, 19 y el nuevo 23).

### 5.2. Resto del backlog (de sesiones anteriores)
1. **Tasa BCV — el daemon 24/7 en Easypanel sigue EN PAUSA (no retomar ese enfoque específico sin que el usuario lo pida explícitamente).** El nuevo enfoque de "refresco perezoso durante el uso del panel" SÍ está en progreso activo (ver sección 3, "Etapa 1 — tasa BCV automática"): completar Tarea 1.11 (prueba real con red+DB — la corre el usuario), luego Etapa 2 (orquestador con throttle de 45 min, `RelojYTasa` haciendo polling, `SelectorMetodoPago`/`ModalRegistrarEgreso` revalidando) y Etapa 3 (validar en el servidor, justo antes de registrar cada cobro en Bs, que se está usando la última tasa publicada — rechazar y pedir reconfirmación si cambió). Seguir `docs/superpowers/plans/2026-09-26-tasa-bcv-automatica.md` etapa por etapa. La Etapa 4 (respaldo con cron/systemd) es opcional y solo con aprobación explícita adicional.
2. **Verificar con el usuario que el botón "Imprimir" de `/pagos` funciona como se espera** (la lista plana en pantalla y la vista de impresión en blanco y negro) — recién implementado, no probado en vivo todavía. Ya está pusheado a `main`; falta que Easypanel redeploye (automático si tiene el deploy automático habilitado, si no, "Forzar reconstrucción" manual en el servicio `gym-app`).
3. **Verificar en vivo el rediseño de `/usuarios`** (tarjetas separadas socios/gerentes/recepción vs. entrenadores, papelera para eliminar, permisos ocultos para entrenadores) — recién implementado, no probado en vivo todavía.
4. **Correr `npm run db:quitar-permisos-entrenadores --workspace packages/db` contra la base real** para limpiar los permisos que hayan quedado en entrenadores creados antes de este cambio (el script ya existe, solo falta ejecutarlo con el `DATABASE_URL` real — esta sesión no tiene acceso a él).
5. **Ojo con "Eliminar" en `/usuarios` sobre un usuario con historial** (turnos, pagos registrados, miembros asignados como entrenador): la base rechaza el borrado físico por integridad referencial y se muestra un error, pero como ya no existe "dar de baja", no hay forma de ocultar/desactivar a ese usuario desde la UI. Si el usuario reporta este caso, decidir si hace falta reintroducir algún mecanismo de baja solo para ese escenario (no pedido explícitamente todavía).
6. **Verificar en vivo con el caso real de Juanito (cédula 585956):** que el cambio de plan de "Sin entrenador" ($25) a "Con entrenador" ($30) ahora cobre $5 y NO mueva el vencimiento, usando la nueva card "Cambiar de plan" en `/miembros/{id}`. Recién implementado, sin probar en vivo.
7. Si en el futuro se necesita permitir "downgrade con reembolso" (diferencia negativa), hoy `CambiarPlanConPago.ts` la trata como $0 (no cobra ni reembolsa) — no pedido todavía, dejarlo así hasta que el usuario lo pida.
8. Si en algún momento se quiere reconsiderar la pantalla de "resumen del turno anterior" al cerrar turno (ver sección 2, se decidió no agregarla), preguntar primero qué caso de uso concreto no cubre la tarjeta "Último cierre" actual antes de implementar nada.
9. **Verificar en vivo que ya no aparecen dos botones "Cambiar plan" en `/miembros/{id}` al mismo tiempo**, y que la opción "Personalizado" desapareció del selector de plan (alta y edición). Recién implementado, sin probar en vivo.
10. **Eliminar los planes "$25 Sede Única" y "$35 VIP Multisede" (pedido por el usuario, sin hacer todavía)** — esta sesión no tiene acceso a la base real. El usuario pidió reemplazar "dar de baja" por "eliminar" directo en la lista de `/planes`, ya implementado (papelera en cada fila/card, `eliminarPlanAction`). Si esos dos planes NO tienen miembros/suscripciones que los usaron, el borrado va a andar directo; si SÍ tienen historial, la base lo va a rechazar (`PlanConHistorialError`) — en ese caso, avisar al usuario que hace falta decidir qué hacer con esos miembros primero (cambiarles el plan) antes de poder borrarlo.
11. **Verificar en vivo el bloqueo de cambio de plan entre frecuencias distintas** (semanal → mensual ya no debería poder cobrar solo diferencia, el selector de "Cambiar de plan" debería listar solo planes de la misma frecuencia) y **la nueva papelera en `/planes`** (eliminar un plan sin historial debería funcionar; uno con historial debería mostrar el error legible en vez de romper). Recién implementado, sin probar en vivo.
12. **Verificar en vivo el panel con pestañas "Registrar pago"/"Cambiar de plan"** en `/miembros/{id}` — confirmar que nunca se ven las dos grillas de método de pago a la vez, y que sin ciclo vigente se sigue mostrando "Registrar pago" directo (sin pestañas). Recién implementado, sin probar en vivo.
13. ~~Pendiente: repensar la reorganización de "Editar miembro"~~ — **HECHO, segunda vez, con mockup aprobado primero y sin repetir el bug del primer intento** (ver punto 14).
14. **Verificar en vivo la reorganización en 3 columnas de "Editar miembro"** (Datos personales | Plan de membresía | Registrar pago/Cambiar de plan, Ciclos abajo como lista) — sobre todo: que "Guardar" sigue guardando bien Datos personales + sede + entrenador + un cambio de plan gratis a medio hacer (todo sigue siendo el mismo `<form>`, solo se reordenó con `display: contents` + CSS Grid, sin sacar ningún campo del árbol del formulario esta vez); que el botón ahora dice "Asignar entrenador"; que "Cobrar diferencia y cambiar plan" redirige a `/miembros` con el mensajito verde de éxito en vez de quedarse en la misma pantalla; y que si hay cambios sin guardar en Datos personales al momento de tocar ese botón, aparece el `window.confirm` de aviso antes de mandarte para afuera. Recién implementado, sin probar en vivo — esta sesión no tiene compilador/typecheck disponible (sin `node_modules`), solo revisión manual del JSX y diffs.
15. **Fix de ancho tras ver la pantalla en vivo: las 3 columnas se veían MUY apretadas** (texto cortado, "Cambiar de plan" partido en dos líneas) porque el contenedor de `/miembros/{id}` y `/miembros/nuevo` seguía en `max-w-4xl` (896px, heredado de cuando el formulario era de 1 sola columna ancha + un aside angosto) — muy poco para 3 columnas cómodas. Se subió a `max-w-7xl` (1280px) en ambas páginas (no se tocó el `max-w-4xl` del aviso de "caja cerrada" en `nuevo/page.tsx`, que es un mensaje simple, no el formulario). **Sin verificar todavía en vivo** — el usuario mandó captura de la versión angosta pero esta sesión no puede ver el resultado del fix hasta que se redeploye.
16. **Verificar en vivo los 4 ajustes del mensaje siguiente** (ver sección 3, "Ajustes tras ver la pantalla ancha en vivo"): que las 3 cards arranquen más arriba sin scroll, que "Guardar" se vea ancho como antes, que "Cambiar de plan" a un plan con entrenador ahora muestre el selector de entrenador y lo exija antes de dejar enviar, y que "Foto de perfil" ya no desborde la card. Recién implementado, sin probar en vivo.
17. **Verificar en vivo el badge rojo "VENCIDO"** en `/miembros` (Cards y Lista) — que aparezca solo en miembros con `fechaVencimiento` en el pasado, al lado del toggle Activo/Inactivo. Recién implementado, sin probar en vivo.
18. **Verificar en vivo el roadmap del cliente, punto (b):** crear un plan "Cortesía" a $0 desde `/planes` (no hace falta migración, ya funcionaba), dar de alta un miembro con ese plan y confirmar que NO pide método de pago y que el pago inicial queda registrado con `metodo: "Cortesía"`; probar también "Registrar pago" normal y el modal de Caja con ese mismo plan más adelante (segundo ciclo). Recién implementado, sin probar en vivo.
19. **Verificar en vivo el roadmap del cliente, punto (c):** crear un entrenador desde `/usuarios/nuevo`, confirmar que no pide email/contraseña, que queda creado, y que se puede asignar normalmente como entrenador de un miembro. Recién implementado, sin probar en vivo.
20. ~~Roadmap del cliente, último punto: (d) pagos fraccionados + pagos mixtos~~ — **HECHO** (ver sección 5.1 y sección 3, detalle técnico completo). Falta verificación en vivo, ver punto 23.
21. **Verificar en vivo el rediseño de `/usuarios/nuevo`** (card contenida en `max-w-md`, campos reordenados, "Sucursales" en 2 columnas) — que se vea bien tanto en desktop como en mobile, y que crear un usuario normal (no entrenador) siga funcionando igual que antes. Recién implementado, sin probar en vivo.
22. ~~Verificar en vivo el fix del bug de "ingresá un valor" al crear miembro con plan de cortesía ($0)~~ — **HECHO, confirmado por el usuario.** Sigue pendiente confirmar, cuando surja la oportunidad, que un plan PAGO normal sigue exigiendo precio > 0 y sigue dejando editar el precio a mano como antes (no debería haberse roto, pero no se verificó ese caso específico en vivo todavía).
23. **Verificar en vivo el roadmap del cliente, punto (d) — pagos fraccionados/mixtos ("abonos"):** desde la ficha de un miembro con ciclo vigente, registrar un primer abono MENOR al precio del plan (ej. $15 de $30) y confirmar: (a) el vencimiento se mueve igual que con un pago completo (acceso inmediato), (b) aparece el banner "Saldo pendiente: $15" en el panel de pago, (c) el campo "Monto" viene precargado con ese saldo. Registrar un segundo abono con OTRO método (ej. pago móvil) por el resto y confirmar que el vencimiento NO se vuelve a mover y el saldo pendiente desaparece. Revisar el historial de pagos del miembro (`/miembros/{id}/pagos`) y confirmar que ambos abonos aparecen como "Abono 1 de 2"/"Abono 2 de 2" junto al rango de ciclo. Probar también el mismo flujo (monto editable) desde el modal de Caja. Recién implementado, verificado contra una base Postgres real en esta sesión (ver sección 3), pero sin probar todavía en la app real en producción.
24. **Verificar en vivo el fix del hallazgo "monto $0 contra plan pago"** (ver sección 3, penúltima entrada): intentar registrar un pago de $0 contra un miembro con plan PAGO (no cortesía) — el sistema debería rechazarlo con el mensaje "El monto tiene que ser mayor a $0 — este plan no es de cortesía" en vez de dar acceso gratis. De paso, confirmar que el flujo normal de cortesía ($0 en un plan que SÍ es de cortesía) sigue funcionando sin este mensaje de error. Recién implementado y verificado contra una base Postgres real en esta sesión, sin probar todavía en producción.
